---
phase: 05-order-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/status/OrderDetails.tsx, src/components/status/index.ts, src/app/status/page.tsx]
autonomous: true
---

<objective>
Create mobile-optimized order details component that displays selected order information.

Purpose: After selecting an order via search, users need to see key details (Kunde, Produkt, Liefertermin) before updating status.
Output: OrderDetails component integrated into /status page, replacing temporary confirmation card.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-order-search/04-02-SUMMARY.md

@src/components/status/OrderSearch.tsx
@src/app/status/page.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrderDetails component</name>
  <files>src/components/status/OrderDetails.tsx, src/components/status/index.ts</files>
  <action>
Create OrderDetails component that displays order information in a card format.

**Component structure:**
- Accept `order: OrderSearchResult` prop (reuse type from OrderSearch)
- Accept `onClear?: () => void` prop for deselecting order
- Display in a white card with rounded corners and border (match existing styling)

**Information to display:**
- Order number (auftragsnummer) as header - bold, prominent
- Kunde section: firma and/or name
- Produkt section: produkttyp
- Liefertermin section: formatted as dd.MM.yyyy using date-fns/locale/de

**Mobile optimizations:**
- Large, readable text (font-semibold for labels, normal for values)
- Adequate padding (p-4)
- Clear visual hierarchy with labeled sections
- Optional "X" button or "Andere Auftrag wählen" link to clear selection

**Styling:**
- Use existing Tailwind classes: bg-white, rounded-lg, border-ghl-border
- Section labels in text-ghl-text-secondary, values in text-ghl-text
- Consistent with OrderSearch dropdown styling

Add export to src/components/status/index.ts barrel file.
  </action>
  <verify>TypeScript compiles without errors (npm run build)</verify>
  <done>OrderDetails component exists with proper typing and styling, exported from index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Integrate OrderDetails into status page</name>
  <files>src/app/status/page.tsx</files>
  <action>
Replace the temporary confirmation card with the new OrderDetails component.

**Changes to status page:**
1. Import OrderDetails from @/components/status
2. Replace the temporary div with CheckCircle icon with OrderDetails component
3. Pass selectedOrder to OrderDetails
4. Add handleClear function to reset selectedOrder to null
5. Pass onClear to OrderDetails

**State flow:**
- When order selected via OrderSearch: setSelectedOrder(order)
- OrderDetails renders with order details
- When user clicks clear: setSelectedOrder(null), search input re-focuses

**Layout:**
- OrderSearch at top
- OrderDetails below when order selected
- Space between components (already have space-y-4)
  </action>
  <verify>
1. npm run build succeeds
2. Navigate to /status
3. Search for an order, select it
4. OrderDetails card appears with Kunde, Produkt, Liefertermin
5. Clear button returns to search-only state
  </verify>
  <done>Status page shows OrderDetails when order selected, clear functionality works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] OrderDetails component renders with all required fields
- [ ] Date formatting uses German locale (dd.MM.yyyy)
- [ ] Mobile touch targets are adequate (buttons min-h-12)
- [ ] Clear functionality deselects order and allows new search
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript or build errors
- OrderDetails displays Kunde, Produkt, Liefertermin clearly
- UI is mobile-optimized and matches existing design patterns
</success_criteria>

<output>
After completion, create `.planning/phases/05-order-selection/05-01-SUMMARY.md`
</output>
