---
phase: 30-test-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/utils/prisma-mock.ts
  - src/__tests__/utils/supabase-mock.ts
  - src/__tests__/fixtures/orders.ts
  - src/__tests__/fixtures/machines.ts
  - .planning/codebase/TESTING.md
autonomous: true
---

<objective>
Complete test infrastructure with mocks and fixtures for API/service layer testing.

Purpose: Jest and Testing Library are already configured (30 tests passing). This plan adds the missing test utilities (Prisma/Supabase mocks, data fixtures) needed for comprehensive API and service layer testing in subsequent phases.

Output: Test utilities directory with mocks and fixtures, updated TESTING.md documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md

# Existing test setup:
@jest.config.js
@jest.setup.js
@src/components/dashboard/__tests__/KPICard.test.tsx
@src/hooks/__tests__/useOrderFormatter.test.ts

# Data layer to mock:
@src/lib/prisma.ts
@src/lib/supabase.ts

# Types for fixtures:
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test utilities and mocks</name>
  <files>src/__tests__/utils/prisma-mock.ts, src/__tests__/utils/supabase-mock.ts, src/__tests__/utils/index.ts</files>
  <action>
Create test utilities directory structure and mocks:

1. `src/__tests__/utils/prisma-mock.ts`:
   - Export `mockPrisma` with jest mocked methods for Order, Machine, OrderHistory
   - Use `jest.mock('@/lib/prisma')` pattern
   - Include `mockReset()` helper for cleanup

2. `src/__tests__/utils/supabase-mock.ts`:
   - Export `mockSupabase` that mocks `createClient` from `@supabase/supabase-js`
   - Mock the chained query pattern: `.from().select().eq().order()` etc.
   - Return configurable mock data

3. `src/__tests__/utils/index.ts`:
   - Re-export all utils for convenient imports
   - Export type helpers if needed

Pattern reference: The existing tests use simple mocks in jest.setup.js for next/navigation. Follow similar patterns.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit src/__tests__/utils/*.ts`</verify>
  <done>Mock utilities created and type-safe</done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures</name>
  <files>src/__tests__/fixtures/orders.ts, src/__tests__/fixtures/machines.ts, src/__tests__/fixtures/index.ts</files>
  <action>
Create test fixtures with realistic sample data:

1. `src/__tests__/fixtures/orders.ts`:
   - Export `createMockOrder(overrides?)` factory function
   - Include orders in different states: offen, in_produktion, fertig, problem
   - Include orders with different versandStatus: null, versandbereit, versendet
   - Match Prisma schema types exactly

2. `src/__tests__/fixtures/machines.ts`:
   - Export `createMockMachine(overrides?)` factory function
   - Include sample machines matching actual Leitmaschinen (Speedmaster XL106, POLAR PACE, etc.)
   - Match Prisma schema types

3. `src/__tests__/fixtures/index.ts`:
   - Re-export all fixtures
   - Export arrays of pre-created fixtures for quick use: `sampleOrders`, `sampleMachines`

Use TypeScript inference from Prisma types for type safety.
  </action>
  <verify>TypeScript compiles and fixtures match Prisma types</verify>
  <done>Fixtures created with realistic sample data covering all states</done>
</task>

<task type="auto">
  <name>Task 3: Update TESTING.md documentation</name>
  <files>.planning/codebase/TESTING.md</files>
  <action>
Update TESTING.md to reflect current state:

1. Change "Status: Not configured" to "Status: Configured (Jest 30 + Testing Library)"

2. Update "Test Framework" section:
   - Jest 30.2.0 with next/jest helper
   - Testing Library React 16.3.1
   - jest-dom 6.9.1 for extended matchers

3. Add "Test Utilities" section documenting:
   - Mock locations: `src/__tests__/utils/`
   - Fixture locations: `src/__tests__/fixtures/`
   - Usage examples for mocking Prisma/Supabase

4. Update "Test File Organization" to reflect actual structure:
   - `__tests__/` directories co-located with source
   - Coverage configuration

5. Update coverage from "0%" to current state (30 tests in 2 suites)

6. Add "Running Tests" section with npm commands
  </action>
  <verify>File contains accurate information about current test setup</verify>
  <done>TESTING.md accurately reflects current test infrastructure</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` still passes (no regressions)
- [ ] `npx tsc --noEmit` passes (no type errors)
- [ ] Mock utilities are importable from `@/__tests__/utils`
- [ ] Fixtures are importable from `@/__tests__/fixtures`
- [ ] TESTING.md reflects actual state
</verification>

<success_criteria>

- All tasks completed
- Test utilities provide working mocks for Prisma and Supabase
- Fixtures cover all order states and machine types
- Documentation is accurate
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-setup/30-01-SUMMARY.md`
</output>
