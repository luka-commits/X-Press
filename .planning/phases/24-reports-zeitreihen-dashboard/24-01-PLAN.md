---
phase: 24-reports-zeitreihen-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/reports/timeseries/route.ts
  - src/components/reports/ThroughputChart.tsx
  - src/components/reports/PipelineDashboard.tsx
  - src/components/reports/index.ts
  - src/components/dashboard/KPICard.tsx
  - src/lib/dashboard-queries.ts
  - src/app/page.tsx
autonomous: true
---

<objective>
Add Zeitreihen chart (Eingang vs Versendet over time) to Reports and remove Problem-KPI from main Dashboard.

Purpose: Complete the Pipeline-Analytics milestone by adding time series visualization and streamlining the Dashboard to remove redundant Problem-KPI (now shown in Reports SnapshotKPIs).
Output: ThroughputChart component with dual-line chart, updated PipelineDashboard, cleaned Dashboard without Problem-KPI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-reports-neu/23-01-SUMMARY.md

# Source files
@src/components/reports/VolumeChart.tsx
@src/components/reports/PipelineDashboard.tsx
@src/components/dashboard/KPICard.tsx
@src/lib/dashboard-queries.ts
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThroughputChart API and Component</name>
  <files>src/app/api/reports/timeseries/route.ts, src/components/reports/ThroughputChart.tsx, src/components/reports/index.ts</files>
  <action>
1. Create API endpoint `/api/reports/timeseries`:
   - Accept `from` and `to` query params (ISO date strings)
   - Return array of `{ date: string, eingang: number, versendet: number }` for each day in range
   - Query Auftrag table:
     - eingang: COUNT where createdAt falls on that date
     - versendet: COUNT where versandStatus='versendet' AND versandUpdatedAt falls on that date
   - Use similar pattern to `/api/reports/pipeline/route.ts`

2. Create ThroughputChart component:
   - Follow VolumeChart.tsx pattern (recharts LineChart)
   - Two lines: eingang (blue #3b82f6) and versendet (green #22c55e)
   - X-axis: dates formatted as dd.MM
   - Y-axis: count (integer)
   - Loading and empty states matching VolumeChart
   - Legend showing "Eingang" and "Versendet"
   - Custom tooltip showing date + both values

3. Export ThroughputChart from components/reports/index.ts
  </action>
  <verify>
- npm run build succeeds
- curl '/api/reports/timeseries?from=2026-01-01&to=2026-01-17' returns valid JSON array
  </verify>
  <done>API returns daily eingang/versendet counts, ThroughputChart renders dual-line chart</done>
</task>

<task type="auto">
  <name>Task 2: Add ThroughputChart to PipelineDashboard</name>
  <files>src/components/reports/PipelineDashboard.tsx</files>
  <action>
1. Import ThroughputChart and its data type from './ThroughputChart'

2. Add state for timeseries data and loading:
   - `timeseriesData` state (array of { date, eingang, versendet })
   - `timeseriesLoading` state

3. Add fetchTimeseriesData function similar to fetchVolumeData:
   - Fetch from `/api/reports/timeseries?from=${fromISO}&to=${toISO}`
   - Set timeseriesData and timeseriesLoading states

4. Call fetchTimeseriesData in the useEffect when dateRange changes

5. Add ThroughputChart to the two-column grid, REPLACING VolumeChart:
   - Left: ThroughputChart (Eingang vs Versendet)
   - Right: PlzChart (PLZ-Verteilung)
   - Update section titles:
     - Left: "Durchfluss Zeitreihe" / "Aufträge pro Tag: Eingang vs. Versendet"

Note: VolumeChart (showing completed orders) is now redundant - ThroughputChart shows versendet which is the same data. Remove VolumeChart from the dashboard.
  </action>
  <verify>
- npm run build succeeds
- Reports page shows ThroughputChart with two lines where VolumeChart was
  </verify>
  <done>ThroughputChart replaces VolumeChart in PipelineDashboard, shows eingang vs versendet over time</done>
</task>

<task type="auto">
  <name>Task 3: Remove Problem-KPI from Dashboard</name>
  <files>src/components/dashboard/KPICard.tsx, src/lib/dashboard-queries.ts, src/app/page.tsx</files>
  <action>
1. In dashboard-queries.ts:
   - Remove `problemOrders: number` from DashboardKPIs interface
   - Remove problem query from getDashboardKPIs() Promise.all
   - Remove `problemOrders: problemResult.count || 0` from return object

2. In KPICard.tsx:
   - Remove `problemOrders: number` from KPICardsGridProps interface
   - Remove Problem-Aufträge KPICard from the grid
   - Change grid from `lg:grid-cols-5` to `lg:grid-cols-4`

3. In page.tsx:
   - Remove `problemOrders={kpis.problemOrders}` prop from KPICardsGrid

Reason: Problem orders are now shown in Reports → SnapshotKPIs (problemAuftraege). Having it on both Dashboard and Reports is redundant. Reports is the appropriate place for this metric.
  </action>
  <verify>
- npm run build succeeds
- npm run lint passes
- Dashboard shows 4 KPI cards (Aktive Aufträge, Bald fällig, Ø Auslastung, Engpass)
- No TypeScript errors
  </verify>
  <done>Dashboard has 4 KPI cards, Problem-KPI removed, no compilation errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] npm run lint passes (no new warnings)
- [ ] ThroughputChart shows two lines for eingang vs versendet
- [ ] Reports page displays new time series chart
- [ ] Dashboard shows 4 KPI cards (no Problem-Aufträge)
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All 3 tasks completed
- ThroughputChart component created and working
- PipelineDashboard shows time series chart
- Dashboard streamlined to 4 KPIs
- Problem-KPI moved exclusively to Reports (where it already exists in SnapshotKPIs)
  </success_criteria>

<output>
After completion, create `.planning/phases/24-reports-zeitreihen-dashboard/24-01-SUMMARY.md`
</output>
