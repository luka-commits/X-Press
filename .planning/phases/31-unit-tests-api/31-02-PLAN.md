---
phase: 31-unit-tests-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/dashboard/kpi-orders/__tests__/kpi-orders.test.ts
  - src/app/api/reports/pipeline/__tests__/pipeline.test.ts
autonomous: true
---

<objective>
Test Dashboard KPI and Reports Pipeline API routes.

Purpose: Ensure KPI drilldown and pipeline analytics endpoints work correctly with proper validation and data aggregation.
Output: 2 test files covering dashboard kpi-orders and reports pipeline APIs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-setup/30-01-SUMMARY.md
@.planning/codebase/TESTING.md
@src/__tests__/utils/supabase-mock.ts
@src/__tests__/fixtures/orders.ts
@src/app/api/dashboard/kpi-orders/route.ts
@src/app/api/reports/pipeline/route.ts
@src/lib/dashboard-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Dashboard KPI Orders API (/api/dashboard/kpi-orders)</name>
  <files>src/app/api/dashboard/kpi-orders/__tests__/kpi-orders.test.ts</files>
  <action>
Create tests for the KPI orders drilldown endpoint:

1. Setup: Mock the dashboard-queries module functions (getOpenOrders, getCriticalOrders, getOverdueOrders, getProblemOrders)
2. Test cases:
   - Returns 400 when type parameter is missing
   - Returns 400 for invalid type values (not total/critical/overdue/problem)
   - Returns orders for type='total' (calls getOpenOrders)
   - Returns orders for type='critical' (calls getCriticalOrders)
   - Returns orders for type='overdue' (calls getOverdueOrders)
   - Returns orders for type='problem' (calls getProblemOrders)
   - Uses provided date parameter
   - Returns 400 for invalid date format
   - Returns correct response shape: { type, orders, count, date }
   - Returns 500 on query errors

Mock dashboard-queries module with jest.mock(). Return sample KPIOrderItem arrays.
Test URL query params: ?type=total&date=2026-01-17
  </action>
  <verify>npm test -- kpi-orders.test.ts passes with all test cases</verify>
  <done>KPI orders API tested: type validation, date parsing, all 4 KPI types, error handling</done>
</task>

<task type="auto">
  <name>Task 2: Test Reports Pipeline API (/api/reports/pipeline)</name>
  <files>src/app/api/reports/pipeline/__tests__/pipeline.test.ts</files>
  <action>
Create tests for the pipeline analytics endpoint:

1. Setup: Mock Supabase using mockSupabase from test utils
2. Test cases:
   - Returns 400 when from parameter is missing
   - Returns 400 when to parameter is missing
   - Returns 400 for invalid date format
   - Returns throughput data with counts and changePercent
   - Returns snapshot data (aktiveAuftraege, problemAuftraege, etc.)
   - Returns periodKpis (avgDaysToShip, onTimePercent, totalShipped)
   - Calculates previous period correctly (equal length before from date)
   - Returns 500 on database errors

Mock Supabase chainable query builder for multiple parallel queries.
The route uses 5 parallel fetches - mock the from().select() pattern.
Test helper functions indirectly through route response.
  </action>
  <verify>npm test -- pipeline.test.ts passes with all test cases</verify>
  <done>Pipeline API tested: date validation, throughput, snapshot, KPIs, period comparison</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- kpi-orders.test.ts passes
- [ ] npm test -- pipeline.test.ts passes
- [ ] npm run lint passes without new errors
- [ ] All test files follow project conventions
</verification>

<success_criteria>

- All 2 test files created
- All tests pass
- Dashboard KPI drilldown fully tested
- Pipeline analytics fully tested (throughput, snapshot, period KPIs)
  </success_criteria>

<output>
After completion, create `.planning/phases/31-unit-tests-api/31-02-SUMMARY.md`
</output>
