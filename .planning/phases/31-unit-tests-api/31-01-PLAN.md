---
phase: 31-unit-tests-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/orders/__tests__/orders.test.ts
  - src/app/api/orders/[id]/status/__tests__/status.test.ts
  - src/app/api/orders/search/__tests__/search.test.ts
autonomous: true
---

<objective>
Test the core Orders API routes: list with filters, status updates, and search.

Purpose: Ensure critical order management endpoints work correctly with proper validation, filtering, and error handling.
Output: 3 test files covering orders list, status update, and search APIs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-setup/30-01-SUMMARY.md
@.planning/codebase/TESTING.md
@src/__tests__/utils/prisma-mock.ts
@src/__tests__/fixtures/orders.ts
@src/app/api/orders/route.ts
@src/app/api/orders/[id]/status/route.ts
@src/app/api/orders/search/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Orders List API (/api/orders)</name>
  <files>src/app/api/orders/__tests__/orders.test.ts</files>
  <action>
Create comprehensive tests for the orders list endpoint:

1. Setup: Mock Prisma using mockPrisma from test utils, reset before each test
2. Test cases:
   - Returns paginated orders with default params (page=1, limit=25)
   - Filters by status ('aktiv', 'abgeschlossen', 'all')
   - Filters by deadline ('today', 'week', 'overdue')
   - Filters by produkttyp and sachbearbeiter
   - Searches across auftragsnummer, produkttyp, kunde.name, kunde.firma
   - Validates and sanitizes sortBy (only allowed columns)
   - Validates sortOrder ('asc', 'desc')
   - Returns 500 on database errors
   - Computes status to 'abgeschlossen' for past liefertermin

Use createMockOrder from fixtures for test data. Mock both findMany and count.
Test URL query params by creating NextRequest with proper URL.
  </action>
  <verify>npm test -- orders.test.ts passes with all test cases</verify>
  <done>Orders list API tested: pagination, filtering, search, sorting, validation, error handling</done>
</task>

<task type="auto">
  <name>Task 2: Test Order Status Update API (/api/orders/[id]/status)</name>
  <files>src/app/api/orders/[id]/status/__tests__/status.test.ts</files>
  <action>
Create tests for the status update PATCH endpoint:

1. Setup: Mock Prisma, reset before each test
2. Test cases:
   - Updates istStatus to valid enum value (in_produktion, fertig, problem)
   - Updates statusKommentar with new status
   - Resets status when istStatus is null
   - Sets statusUpdatedAt to current timestamp
   - Returns 400 when istStatus is undefined
   - Returns 400 for invalid istStatus values
   - Returns 404 when order not found
   - Returns 500 on database errors

Mock findUnique to check existence, update to perform change.
Test with NextRequest containing JSON body and dynamic params.
  </action>
  <verify>npm test -- status.test.ts passes with all test cases</verify>
  <done>Status update API tested: valid updates, validation errors, not found, error handling</done>
</task>

<task type="auto">
  <name>Task 3: Test Orders Search API (/api/orders/search)</name>
  <files>src/app/api/orders/search/__tests__/search.test.ts</files>
  <action>
Create tests for the lightweight search endpoint:

1. Setup: Mock Prisma, reset before each test
2. Test cases:
   - Returns empty results for query less than 2 characters
   - Returns empty results for missing query parameter
   - Searches across auftragsnummer, produkttyp, kunde.name, kunde.firma
   - Only returns active orders (status='aktiv')
   - Limits results to 10
   - Orders by liefertermin ascending
   - Returns 500 on database errors

Mock findMany with select fields matching the route's response structure.
Verify the minimal response shape (OrderSearchResult interface).
  </action>
  <verify>npm test -- search.test.ts passes with all test cases</verify>
  <done>Search API tested: min length, active filter, limit, sort, error handling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- orders.test.ts passes
- [ ] npm test -- status.test.ts passes
- [ ] npm test -- search.test.ts passes
- [ ] npm run lint passes without new errors
- [ ] All test files follow project conventions (German domain terms, TypeScript)
</verification>

<success_criteria>

- All 3 test files created
- All tests pass
- Core orders functionality covered: list, filter, sort, search, status update
- Error cases covered: validation, not found, database errors
  </success_criteria>

<output>
After completion, create `.planning/phases/31-unit-tests-api/31-01-SUMMARY.md`
</output>
