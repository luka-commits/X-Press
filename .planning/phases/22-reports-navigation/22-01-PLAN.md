---
phase: 22-reports-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/layout/Sidebar.tsx, src/app/api/reports/completed/route.ts, src/app/reports/page.tsx]
autonomous: true
---

<objective>
Add Reports to sidebar navigation and restrict Abgeschlossene tab to only show shipped orders (versandStatus='versendet').

Purpose: Complete system consolidation with clear navigation and proper data separation - Reports now accessible from main navigation, and Abgeschlossene only shows fully completed orders that have been shipped.
Output: Updated Sidebar with Reports link, Abgeschlossene tab shows only shipped orders.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-pipeline-view/21-01-SUMMARY.md

@src/components/layout/Sidebar.tsx
@src/app/api/reports/completed/route.ts
@src/app/reports/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Reports to Sidebar navigation</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <action>Add Reports item to the navigation array after Versand:
```typescript
{ name: "Reports", href: "/reports" },
```
This makes the Reports page accessible from the main sidebar navigation alongside Dashboard, Aufträge, Kalender, and Versand.</action>
  <verify>npm run build succeeds, navigate to /reports via sidebar link</verify>
  <done>Reports appears in sidebar navigation and links to /reports page</done>
</task>

<task type="auto">
  <name>Task 2: Restrict Abgeschlossene to shipped orders only</name>
  <files>src/app/api/reports/completed/route.ts, src/app/reports/page.tsx</files>
  <action>1. In /api/reports/completed/route.ts:
   - Change the query filter from `.or('istStatus.eq.fertig,versandStatus.eq.versendet')` to `.eq('versandStatus', 'versendet')`
   - Update the JSDoc comment to reflect the new behavior: "Returns orders where versandStatus='versendet'"
   - Simplify completedAt logic since all orders now have versandUpdatedAt

2. In /app/reports/page.tsx:
   - Update the comment on line 27 from "Completed orders (istStatus='fertig' OR versandStatus='versendet')" to "Shipped orders (versandStatus='versendet')"

The rationale: "Abgeschlossene" (completed) should only show orders that have been shipped - orders with istStatus='fertig' but not yet shipped belong in the Aufträge tab with versandStatus filtering.</action>
  <verify>npm run build succeeds, /api/reports/completed returns only versandStatus='versendet' orders</verify>
  <done>Abgeschlossene tab shows only shipped orders, API filter updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Sidebar shows Reports link and it navigates to /reports
- [ ] /api/reports/completed only returns orders with versandStatus='versendet'
- [ ] Comments in code reflect the new behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Reports accessible from sidebar navigation
- Abgeschlossene shows only shipped orders
  </success_criteria>

<output>
After completion, create `.planning/phases/22-reports-navigation/22-01-SUMMARY.md`
</output>
