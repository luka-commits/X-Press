---
phase: 09-versand-datenmodell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/xml-parser.ts
  - src/lib/import-service.ts
autonomous: true
---

<objective>
Extend database schema with VersandStatus enum and structured address fields for shipping workflow.

Purpose: Enable Phase 10-12 features (Versand-API, PLZ-Sortierung, Kartenansicht) by providing the data foundation.
Output: Updated Prisma schema with shipping status tracking and individual address fields (PLZ, Ort, Strasse).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-schema/01-01-SUMMARY.md

@prisma/schema.prisma
@src/lib/xml-parser.ts
@src/lib/import-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VersandStatus enum and shipping fields to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>
Add VersandStatus enum with three values following IstStatus pattern:
- offen (not yet ready for shipping - default)
- versandbereit (ready to be shipped)
- versendet (shipped)

Add shipping status fields to Auftrag model (following IstStatus pattern):
- versandStatus: VersandStatus? (nullable, default null)
- versandKommentar: String? (optional comment from shipping team)
- versandUpdatedAt: DateTime? (when shipping status was last changed)

Add individual address fields to Auftrag model for PLZ sorting and map clustering:
- lieferStrasse: String?
- lieferPlz: String?
- lieferOrt: String?
- lieferLand: String?

Place VersandStatus enum after IstStatus enum. Place new Auftrag fields in a new comment block "// Versand Status fields (Versand-Team Workflow)" after the Mobile Status-Update fields block.
  </action>
  <verify>npx prisma validate passes</verify>
  <done>VersandStatus enum defined, 7 new fields added to Auftrag model, schema validates</done>
</task>

<task type="auto">
  <name>Task 2: Extend XML parser and import service for address fields</name>
  <files>src/lib/xml-parser.ts, src/lib/import-service.ts</files>
  <action>
In xml-parser.ts:
1. Add individual address fields to ParsedKunde interface:
   - strasse: string | null
   - plz: string | null
   - ort: string | null
   - land: string | null
   (Keep existing combined `adresse` field for backwards compatibility)

2. In parseXML(), extract individual fields from kundeNode (already available as @_Strasse, @_PLZ, @_Ort, @_LKZ/LandName):
   - strasse: kundeNode['@_Strasse'] || null
   - plz: kundeNode['@_PLZ'] || null
   - ort: kundeNode['@_Ort'] || null
   - land: kundeNode['@_LandName'] || kundeNode['@_LKZ'] || null

In import-service.ts:
3. Extend the Auftrag upsert to save individual address fields:
   - lieferStrasse: parsed.kunde?.strasse
   - lieferPlz: parsed.kunde?.plz
   - lieferOrt: parsed.kunde?.ort
   - lieferLand: parsed.kunde?.land

Note: Customer address serves as delivery address in this system (no separate Lieferadresse in XML).
  </action>
  <verify>npm run build passes</verify>
  <done>XML parser extracts individual address fields, import service saves them to Auftrag</done>
</task>

<task type="auto">
  <name>Task 3: Run database migration</name>
  <files>prisma/schema.prisma</files>
  <action>
Run `npx prisma db push` to sync schema to database (following Phase 1 pattern - using db push instead of migrate dev due to existing database drift).

Then verify Prisma client regenerated with:
1. VersandStatus enum available
2. New fields available on Auftrag type
  </action>
  <verify>npx prisma validate passes, Prisma client types include VersandStatus and new fields</verify>
  <done>Database schema synced, VersandStatus enum and address fields available in @prisma/client</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx prisma validate passes
- [ ] npm run build succeeds
- [ ] VersandStatus enum visible in @prisma/client
- [ ] New Auftrag fields (versandStatus, lieferPlz, etc.) available in TypeScript
</verification>

<success_criteria>

- All 3 tasks completed
- VersandStatus enum added with offen, versandbereit, versendet values
- 7 new fields on Auftrag model (3 versand status + 4 address)
- XML parser extracts PLZ, Ort, Strasse, Land individually
- Import service saves individual address fields to Auftrag
- Database synced via prisma db push
- TypeScript builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-versand-datenmodell/09-01-SUMMARY.md`
</output>
