---
phase: 15-routenplanung-basis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/versand/VersandOrderList.tsx
  - src/components/versand/VersandOrderCard.tsx
  - src/components/map/DeliveryMap.tsx
autonomous: true
---

<objective>
Implement route planning foundation on the Versand page with mode toggle, order selection, numbered markers, and route line visualization.

Purpose: Enable Versand team to visually plan delivery routes by selecting orders in desired sequence.
Output: Working route planning mode on /versand with visual feedback on both list and map.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-routenplanung-basis/15-CONTEXT.md
@.planning/phases/14-karte-liste-interaktion/14-01-SUMMARY.md

# Current implementation to extend:
@src/components/versand/VersandOrderList.tsx
@src/components/versand/VersandOrderCard.tsx
@src/components/map/DeliveryMap.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route planning mode toggle and state</name>
  <files>src/components/versand/VersandOrderList.tsx</files>
  <action>
Add route planning mode to VersandOrderList:

1. Add state:
   - `routePlanningMode: boolean` (default false)
   - `routeOrders: string[]` (ordered array of auftragsnummern)

2. Add toggle button in the filter bar (after deadline filter):
   - Icon: `Route` from lucide-react
   - Label: "Routenplanung"
   - Active state: blue styling matching other active filters
   - Clicking toggles mode, exiting mode clears routeOrders

3. Add helper to manage route selection:
   - `handleRouteOrderToggle(orderId: string)`:
     - If already in routeOrders, remove it (filter out)
     - If not in routeOrders, append to end
   - Maintains selection order (first clicked = stop 1)

4. Pass new props to child components:
   - VersandOrderCard: routePlanningMode, routePosition (1-based or null), onRouteToggle
   - DeliveryMap: routePlanningMode, routeOrders (full ordered array of mappable orders)

Style the toggle button consistently with existing filter chips but make it visually distinct (e.g., with Route icon, slightly separated).
  </action>
  <verify>npm run build passes, toggle button appears in filter bar, mode can be toggled</verify>
  <done>Route planning mode toggles on/off, selection state tracked</done>
</task>

<task type="auto">
  <name>Task 2: Add checkboxes and selection to VersandOrderCard</name>
  <files>src/components/versand/VersandOrderCard.tsx</files>
  <action>
Extend VersandOrderCard to support route planning mode:

1. Add props:
   - `routePlanningMode?: boolean`
   - `routePosition?: number | null` (1-based position in route, null if not selected)
   - `onRouteToggle?: () => void`

2. When routePlanningMode is true:
   - Show checkbox on left side of card (before content)
   - Checkbox checked = order is in route
   - Click anywhere on card triggers onRouteToggle (not just checkbox)
   - Show route position badge (e.g., "1", "2", "3") next to checkbox when selected

3. Visual states:
   - Selected for route: blue left border (or blue ring), position badge visible
   - Not selected: normal card appearance
   - Position badge: small circle with number, blue background, white text

4. Disable normal selection behavior when in route planning mode:
   - Don't trigger onSelect (the expand/collapse behavior)
   - Only handle route toggle clicks

Keep the existing id={`order-card-${order.auftragsnummer}`} for scroll-to functionality.
  </action>
  <verify>npm run build passes, checkboxes appear in route mode, clicking toggles selection with position badge</verify>
  <done>Cards show checkboxes in route mode, selection toggles with numbered badges</done>
</task>

<task type="auto">
  <name>Task 3: Add route visualization to DeliveryMap</name>
  <files>src/components/map/DeliveryMap.tsx</files>
  <action>
Extend DeliveryMap to visualize selected route:

1. Add props:
   - `routePlanningMode?: boolean`
   - `routeOrders?: MapOrder[]` (ordered array of selected orders)

2. When routePlanningMode is true with routeOrders:
   - Disable MarkerClusterer (show individual markers)
   - For route orders: show numbered markers (1, 2, 3...) instead of default pins
     - Use custom icon: blue circle with white number text
     - Size slightly larger than default (scale: 20-24)
   - For non-route orders: show semi-transparent gray markers
   - Draw Polyline connecting route stops in order:
     - Use google.maps.Polyline
     - Color: blue (#3B82F6)
     - Weight: 3-4px
     - Dashed or solid (solid recommended for clarity)

3. Numbered marker implementation:
   - Use SVG symbol path or AdvancedMarkerElement label
   - Simple approach: marker.setLabel({ text: "1", color: "white" }) with blue circle icon
   - Numbers correspond to position in routeOrders array (1-indexed)

4. Polyline implementation:
   - Create google.maps.Polyline with path from routeOrders coordinates
   - Store ref to clear/update when routeOrders changes
   - Clean up polyline on unmount or when exiting route mode

5. When routePlanningMode is false:
   - Restore normal clustered view
   - Remove polyline
   - Remove numbered markers

Keep existing selectedOrderId/onOrderSelect functionality for non-route mode.
  </action>
  <verify>npm run build passes, numbered markers appear for route orders, polyline connects stops</verify>
  <done>Map shows numbered markers and route line when orders are selected in route planning mode</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without TypeScript errors
- [ ] Toggle "Routenplanung" button appears in filter bar
- [ ] Clicking toggle enters route planning mode
- [ ] Checkboxes appear on order cards in route mode
- [ ] Selecting orders shows numbered badges (1, 2, 3...)
- [ ] Map shows numbered markers for selected orders
- [ ] Polyline connects selected stops in selection order
- [ ] Exiting route mode returns to normal view
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Route planning mode provides clear visual feedback
- Selection order is preserved and reflected in both list and map
- No regressions to existing Versand page functionality
</success_criteria>

<output>
After completion, create `.planning/phases/15-routenplanung-basis/15-01-SUMMARY.md`
</output>
