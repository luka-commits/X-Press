---
phase: 27-kpi-overlay-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/KPIOrdersDialog.tsx
  - src/components/dashboard/KPICard.tsx
autonomous: true
---

<objective>
Make KPI cards clickable to open a dialog showing the associated order list.

Purpose: Allow users to drill down from dashboard KPIs to see the actual orders behind the numbers.
Output: Clicking "Offene Aufträge" or "Bald fällig" KPI cards opens a dialog with the corresponding orders.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dialog-komponente/25-01-SUMMARY.md
@.planning/phases/26-kpi-detail-api/26-01-SUMMARY.md

# Source files
@src/components/dashboard/KPICard.tsx
@src/components/dashboard/CriticalOrdersList.tsx
@src/components/ui/dialog.tsx
@src/app/api/dashboard/kpi-orders/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KPIOrdersDialog component</name>
  <files>src/components/dashboard/KPIOrdersDialog.tsx</files>
  <action>
Create a new dialog component that:
- Accepts props: kpiType ('total' | 'critical'), title, isOpen, onOpenChange
- Uses Dialog, DialogContent, DialogHeader, DialogTitle from @/components/ui/dialog
- Fetches orders from /api/dashboard/kpi-orders?type={kpiType} when opened (use useState + useEffect)
- Shows loading state while fetching
- Displays orders in a table matching CriticalOrdersList pattern:
  - Columns: Auftrag (link to /orders/{auftragsnummer}), Kunde, Produkttyp, Liefertermin, Tage übrig
  - Use same styling classes as CriticalOrdersList
  - Format liefertermin with date-fns (dd.MM.yyyy, de locale)
  - Color-code tageUebrig badge (red=0, yellow=1, green=2+)
- Show empty state if no orders
- Dialog max-width should be larger than default (max-w-3xl) for table readability
- Add "use client" directive (fetching with useEffect)
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>KPIOrdersDialog component exists and builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Make KPICards clickable and integrate dialogs</name>
  <files>src/components/dashboard/KPICard.tsx</files>
  <action>
Modify KPICard and KPICardsGrid:

1. KPICard changes:
   - Add optional onClick prop: onClick?: () => void
   - When onClick is provided:
     - Add cursor-pointer class
     - Wrap content in button or make div clickable
     - Add hover:border-ghl-blue for clickable cards
   - Keep existing styling for non-clickable cards

2. KPICardsGrid changes:
   - Add "use client" directive (needed for state)
   - Add useState for dialog state: { type: 'total' | 'critical' | null, isOpen: boolean }
   - Import KPIOrdersDialog
   - Add onClick to "Offene Aufträge" card that opens dialog with type='total'
   - Add onClick to "Bald fällig" card that opens dialog with type='critical'
   - Keep "Ø Auslastung" and "Engpass" cards as-is (no onClick)
   - Render KPIOrdersDialog at bottom of grid with appropriate title:
     - type='total': title="Offene Aufträge"
     - type='critical': title="Bald fällig (≤2 Tage)"
  </action>
  <verify>npm run build succeeds, npm run dev works, clicking KPI cards opens dialog</verify>
  <done>Clicking "Offene Aufträge" or "Bald fällig" opens a dialog showing the orders</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] npm run dev starts successfully
- [ ] Clicking "Offene Aufträge" KPI card opens dialog with all open orders
- [ ] Clicking "Bald fällig" KPI card opens dialog with critical orders
- [ ] Dialog shows correct order count matching the KPI number
- [ ] Order links navigate to /orders/{auftragsnummer}
- [ ] "Ø Auslastung" and "Engpass" cards are NOT clickable
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript or build errors
- Dialog displays orders correctly with proper formatting
- User can drill down from KPI to order details
</success_criteria>

<output>
After completion, create `.planning/phases/27-kpi-overlay-ui/27-01-SUMMARY.md`
</output>
