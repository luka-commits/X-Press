---
phase: 37-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.setup.d.ts
  - src/app/api/orders/__tests__/orders.test.ts
  - src/components/reports/ReportsDashboard.tsx
  - src/lib/reporting-queries.ts
autonomous: true
---

<objective>
Fix TypeScript test type errors and clean up ESLint import order warnings.

Purpose: Ensure all TypeScript checks pass and lint is clean for CI/CD.
Output: Zero TypeScript errors, minimal ESLint warnings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current state from analysis:
# - ~143 TypeScript errors (all in test files - jest-dom matchers not recognized)
# - After lint --fix, only 5 warnings remain (3 import order, 2 no-explicit-any)
# - TypeScript strict mode already enabled in tsconfig.json

@jest.config.js
@jest.setup.js
@tsconfig.json
@.eslintrc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jest-dom type declarations</name>
  <files>jest.setup.d.ts</files>
  <action>
Create `jest.setup.d.ts` in project root with:
```typescript
/// <reference types="@testing-library/jest-dom" />
```

This tells TypeScript about the jest-dom matchers (toBeInTheDocument, toHaveClass, etc.) that are imported at runtime in jest.setup.js.

Also add the file to tsconfig.json include array if not already covered by the `**/*.ts` pattern.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -c "toBeInTheDocument" returns 0</verify>
  <done>All jest-dom matcher TypeScript errors resolved</done>
</task>

<task type="auto">
  <name>Task 2: Run ESLint auto-fix</name>
  <files>src/**/*.ts, src/**/*.tsx</files>
  <action>
Run `npm run lint -- --fix` to auto-fix all import order issues.

After the fix, most of the 138 import/order warnings will be resolved automatically by ESLint.
  </action>
  <verify>npm run lint 2>&1 | grep -c "Warning" shows significantly fewer warnings (target: <10)</verify>
  <done>ESLint auto-fixable issues resolved</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining import order issues manually</name>
  <files>src/app/api/orders/__tests__/orders.test.ts, src/components/reports/ReportsDashboard.tsx, src/lib/reporting-queries.ts</files>
  <action>
After auto-fix, these files may still have import order issues that require manual intervention:

1. `orders.test.ts` - Move next/server import before @/__tests__/fixtures import
2. `ReportsDashboard.tsx` - Remove empty line within import group
3. `reporting-queries.ts` - Move ./supabase import after type import

Follow the import order rules:
- Groups: builtin → external → internal → parent → sibling → index
- Newlines between groups
- Alphabetical within groups
  </action>
  <verify>npm run lint 2>&1 | grep "import/order" returns empty (no import order warnings)</verify>
  <done>All import order warnings resolved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npm run lint` shows only the 2 no-explicit-any warnings (to be addressed in 37-02)
- [ ] `npm test` still passes
</verification>

<success_criteria>

- All tasks completed
- TypeScript errors: 0
- ESLint import/order warnings: 0
- Tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/37-code-quality/37-01-SUMMARY.md`
</output>
