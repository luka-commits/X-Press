---
phase: 37-code-quality
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - .eslintrc.json
  - src/app/api/test-parser/route.ts
  - src/components/reports/StageDistributionChart.tsx
  - src/lib/xml-parser.ts
  - src/lib/dashboard-queries.ts
  - src/lib/calendar-queries.ts
  - src/lib/reporting-queries.ts
  - src/app/api/reports/completed/route.ts
autonomous: true
---

<objective>
Upgrade ESLint configuration to strict mode and clean up type suppressions.

Purpose: Enforce code quality through stricter linting rules and reduce eslint-disable comments.
Output: Stricter ESLint config, fewer type suppressions, cleaner codebase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-code-quality/37-01-SUMMARY.md

# Current state:
# - 8 eslint-disable comments for no-explicit-any across 5 files
# - 2 remaining ESLint warnings for no-explicit-any (test-parser, StageDistributionChart)
# - ESLint rules: no-explicit-any="warn", react-hooks/exhaustive-deps="warn"

@.eslintrc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix explicit any warnings in non-suppressed files</name>
  <files>src/app/api/test-parser/route.ts, src/components/reports/StageDistributionChart.tsx</files>
  <action>
Fix the 2 files with `@typescript-eslint/no-explicit-any` warnings that are NOT currently suppressed:

1. `src/app/api/test-parser/route.ts:71:53` - The `any` is likely in XML parsing output
   - Create appropriate type for the parsed XML structure
   - Or use `unknown` with type narrowing

2. `src/components/reports/StageDistributionChart.tsx:9:20` - The `any` is likely in Recharts props
   - Create interface for chart data or use Recharts built-in types
   - Check Recharts documentation for proper typing

Use `unknown` instead of `any` where possible, then narrow the type with type guards.
  </action>
  <verify>npm run lint 2>&1 | grep -c "no-explicit-any" returns 0</verify>
  <done>No more ESLint warnings for explicit any</done>
</task>

<task type="auto">
  <name>Task 2: Review and clean up eslint-disable comments</name>
  <files>src/lib/xml-parser.ts, src/lib/dashboard-queries.ts, src/lib/calendar-queries.ts, src/lib/reporting-queries.ts, src/app/api/reports/completed/route.ts</files>
  <action>
Review the 8 eslint-disable comments and either:
A) Fix the underlying type issue (preferred)
B) Add a justifying comment explaining why the suppression is necessary

Current suppressions:
- `src/lib/xml-parser.ts:1` - File-wide disable (this is too broad - narrow to specific lines)
- `src/lib/xml-parser.ts:236,238` - Line-specific disables
- `src/lib/dashboard-queries.ts:70,230` - Supabase nested relation typing
- `src/lib/calendar-queries.ts:247` - Supabase relation typing
- `src/lib/reporting-queries.ts:237` - Supabase relation typing
- `src/app/api/reports/completed/route.ts:69` - Route handler

For Supabase relation typing issues:
- Consider creating typed response wrappers
- Or use `as const satisfies` patterns
- Document why the suppression is necessary if unfixable

For xml-parser.ts:
- Remove the file-wide disable
- Add specific line disables with comments only where truly necessary
- The fast-xml-parser output is inherently untyped, so some `any` may be unavoidable
  </action>
  <verify>grep -r "eslint-disable" src/ | grep -c "no-explicit-any" is reduced OR all remaining have justifying comments</verify>
  <done>eslint-disable comments reviewed, reduced where possible, justified where necessary</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade ESLint warnings to errors</name>
  <files>.eslintrc.json</files>
  <action>
Update `.eslintrc.json` to make the configuration stricter:

1. Change `@typescript-eslint/no-explicit-any` from "warn" to "error"
   - This enforces proper typing going forward
   - All current violations must be fixed first (Task 1 & 2)

2. Keep `react-hooks/exhaustive-deps` as "warn" for now
   - Too disruptive to change to error without reviewing all hook usages

3. Keep `import/order` as "warn"
   - Auto-fixable, doesn't need to block builds

After changes, verify the build still passes.
  </action>
  <verify>npm run lint exits with 0 (no errors), npm run build succeeds</verify>
  <done>ESLint configured with stricter rules, codebase passes all checks</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes with zero errors (warnings OK)
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] No new eslint-disable comments added (same or fewer than before)
</verification>

<success_criteria>

- All tasks completed
- ESLint errors: 0
- no-explicit-any upgraded to error
- eslint-disable comments reduced or documented
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37-code-quality/37-02-SUMMARY.md`
</output>
