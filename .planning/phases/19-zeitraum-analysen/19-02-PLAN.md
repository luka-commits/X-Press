---
phase: 19-zeitraum-analysen
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/reports/VolumeChart.tsx
  - src/components/reports/AnalyticsView.tsx
  - src/components/reports/index.ts
  - src/app/reports/page.tsx
autonomous: true
---

<objective>
Create VolumeChart component and integrate analytics view into the reports page with tab switching.

Purpose: Complete the Zeitraum-Analysen feature with interactive charts showing order volume trends over time.
Output: Working analytics tab in /reports with date range picker and volume trend chart.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-zeitraum-analysen/19-RESEARCH.md
@.planning/phases/19-zeitraum-analysen/19-01-SUMMARY.md

@src/components/dashboard/CapacityChart.tsx
@src/components/reports/DateRangePicker.tsx
@src/app/api/reports/analytics/route.ts
@src/app/reports/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VolumeChart component following CapacityChart pattern</name>
  <files>src/components/reports/VolumeChart.tsx, src/components/reports/index.ts</files>
  <action>
1. Create VolumeChart component at src/components/reports/VolumeChart.tsx:
   - 'use client' directive required
   - Import from recharts: LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer
   - Import format from date-fns, de locale from date-fns/locale
   - Import Loader2 from lucide-react for loading state

2. Component interface:
   ```typescript
   interface VolumeData {
     date: string;  // 'yyyy-MM-dd' format
     count: number;
   }

   interface VolumeChartProps {
     data: VolumeData[];
     loading?: boolean;
   }
   ```

3. Implement component (FOLLOW CapacityChart.tsx pattern exactly):
   - Loading state: Show centered Loader2 spinner with "Lade Daten..." text
   - Empty state: Show "Keine Daten für diesen Zeitraum" message
   - Chart wrapper: div with style={{ width: '100%', height: 300, minHeight: 300 }}
   - ResponsiveContainer: width="100%" height={300} minHeight={300}
   - LineChart with margin={{ top: 5, right: 30, left: 20, bottom: 5 }}
   - CartesianGrid with strokeDasharray="3 3"
   - XAxis: dataKey="date", tickFormatter to format as "dd.MM" using date-fns with de locale
   - YAxis: integer ticks for order counts
   - Tooltip: custom content showing full date (format: "EEEE, dd. MMMM") and count as "X Aufträge"
   - Line: type="monotone", dataKey="count", stroke="#3b82f6" (blue), strokeWidth={2}, dot={false}

4. Export VolumeChart from src/components/reports/index.ts

AVOID: Don't deviate from CapacityChart ResponsiveContainer pattern - it solves the zero-height issue.
  </action>
  <verify>npm run build succeeds. VolumeChart component accepts data array and renders line chart.</verify>
  <done>VolumeChart component created with loading/empty states and line chart visualization.</done>
</task>

<task type="auto">
  <name>Task 2: Create AnalyticsView component and integrate into reports page with tab switching</name>
  <files>src/components/reports/AnalyticsView.tsx, src/components/reports/index.ts, src/app/reports/page.tsx</files>
  <action>
1. Create AnalyticsView component at src/components/reports/AnalyticsView.tsx:
   - 'use client' directive required
   - Import DateRangePicker, VolumeChart from same directory
   - Import useState, useEffect, useCallback from react
   - Import subDays, format from date-fns

2. Component state:
   - dateRange: { from: Date, to: Date } - default to last 30 days
   - data: VolumeData[] - chart data from API
   - loading: boolean
   - error: string | null

3. Data fetching:
   - useEffect triggered by dateRange changes
   - Fetch from /api/reports/analytics?from={ISO}&to={ISO}
   - Set loading true before fetch, false after
   - Parse JSON response and set data
   - Handle errors gracefully

4. UI layout:
   - Wrapper div with className="space-y-6"
   - Header row: DateRangePicker on the right (flex justify-end)
   - Card section: bg-white rounded-lg p-6 border containing:
     - Title: "Auftragsvolumen" (h3, font-semibold)
     - Subtitle: "Abgeschlossene Aufträge pro Tag" (text-sm, text-neutral-500)
     - VolumeChart with data and loading props
   - Error state: Show red error message if fetch fails

5. Export AnalyticsView from src/components/reports/index.ts

6. Update src/app/reports/page.tsx for tab switching:
   - Add useState for activeTab: 'abgeschlossene' | 'zeitraum' | 'versand'
   - Update tabs array: remove 'active' property, use state instead
   - Enable 'zeitraum' tab (disabled: false)
   - Tab button onClick: setActiveTab(tab.id)
   - Tab styling: active state based on activeTab === tab.id
   - Conditional rendering in content area:
     - activeTab === 'abgeschlossene': <CompletedOrdersTable />
     - activeTab === 'zeitraum': <AnalyticsView />
     - 'versand' tab remains disabled for Phase 20

AVOID: Don't create separate routes for tabs - keep single page with tab state (simpler, matches current pattern).
  </action>
  <verify>
1. npm run build succeeds
2. Visit /reports - "Abgeschlossene" tab shows completed orders table
3. Click "Zeitraum-Analysen" tab - shows date picker and volume chart
4. Change date range - chart updates with new data
  </verify>
  <done>AnalyticsView integrated into reports page with working tab switching between Abgeschlossene and Zeitraum-Analysen.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Tab switching works between Abgeschlossene and Zeitraum-Analysen
- [ ] DateRangePicker presets work (Letzte 7/30 Tage, etc.)
- [ ] VolumeChart displays data correctly with German date formatting
- [ ] Chart handles loading and empty states
</verification>

<success_criteria>

- All tasks completed
- Zeitraum-Analysen tab fully functional
- Chart shows order volume trends for selected date range
- Tab switching preserves state appropriately
- No TypeScript errors introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/19-zeitraum-analysen/19-02-SUMMARY.md`
</output>
