---
phase: 06-status-update-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/components/status/StatusButtons.tsx, src/app/status/page.tsx]
autonomous: false
---

<objective>
Add optional comment field and success/error feedback to status updates.

Purpose: Complete the status update UX with comment support (required for "Problem") and clear feedback.
Output: Full status update flow with comments and visual confirmation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-status-update-ui/06-01-SUMMARY.md

# Source files after Plan 01:
@src/components/status/StatusButtons.tsx
@src/app/status/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comment field with conditional display</name>
  <files>src/components/status/StatusButtons.tsx</files>
  <action>
Add optional comment textarea to StatusButtons component:

1. Add textarea below the 3 buttons with:
   - Placeholder: "Kommentar (optional)" for normal statuses
   - Placeholder: "Bitte Problem beschreiben..." for "Problem" status
   - min-h-20 (80px), resize-none, full width
   - Same styling as other inputs (rounded-lg, border-ghl-border)

2. Behavior:
   - Textarea always visible but collapsed (1-2 rows) by default
   - When "Problem" button clicked, expand textarea and focus it
   - Require comment before submitting "Problem" status (show validation message)
   - Optional for "In Produktion" and "Fertig" statuses

3. Update props:
   - Add onStatusChange(status: IstStatus, comment?: string) callback signature
   - Pass comment to API call via parent component

4. State management:
   - Track which status button was selected (for "Problem" validation)
   - Track comment text
   - Clear comment after successful submission
  </action>
  <verify>npm run build succeeds, TypeScript compiles</verify>
  <done>Comment textarea added, required for "Problem", optional for others, clears on submit</done>
</task>

<task type="auto">
  <name>Task 2: Add success/error feedback</name>
  <files>src/app/status/page.tsx, src/components/status/StatusButtons.tsx</files>
  <action>
Add visual feedback after status update:

1. Create simple inline feedback (no external toast library):
   - Success: Green banner/message "Status aktualisiert" with checkmark icon
   - Error: Red banner/message with error text from API response
   - Show for 3 seconds, then auto-dismiss

2. Implement in status page:
   - Add feedback state: { type: 'success' | 'error', message: string } | null
   - Show feedback banner above or below StatusButtons
   - Auto-dismiss with setTimeout

3. Handle API response:
   - Success (200): Show success feedback, optionally clear selection or keep for next update
   - Error (400, 404, 500): Show error message from response body

4. After successful update:
   - Keep order selected (user may want to update again)
   - Clear the comment field
   - Show success feedback briefly

Use simple approach - no external dependencies. Lucide icons (Check, X) for feedback icons.
  </action>
  <verify>
1. npm run dev - visit /status
2. Select an order and click "Fertig" - see success message
3. Click "Problem" without comment - see validation error
4. Add comment, click "Problem" - see success message
5. Comment field clears after successful submit
  </verify>
  <done>Success/error feedback shows after status updates, validation works for Problem status</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete status update flow: 3 buttons, optional comment (required for Problem), success/error feedback</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/status
    3. Search for an order (e.g., "GP25")
    4. Select an order from results
    5. Test "In Produktion" button:
       - Click without comment → should succeed
       - Success message appears briefly
    6. Test "Fertig" button:
       - Click without comment → should succeed
    7. Test "Problem" button:
       - Click without comment → should show validation error
       - Add comment "Test problem" → click again → should succeed
    8. Verify comment clears after successful submission
    9. Verify order stays selected for potential next update
    10. Mobile: Test on phone or DevTools mobile view
        - Buttons are large enough (56px+ height)
        - Easy to tap without misclicks
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] npm run lint passes
- [ ] Comment field appears below buttons
- [ ] "Problem" requires comment, others optional
- [ ] Success feedback shows after update
- [ ] Error feedback shows on API error
- [ ] Comment clears after successful submit
- [ ] Mobile-friendly (large touch targets)
</verification>

<success_criteria>
- Comment field integrated with conditional requirement
- Visual feedback on success and error
- Validation prevents submitting Problem without comment
- Complete flow verified by human on mobile/responsive view
</success_criteria>

<output>
After completion, create `.planning/phases/06-status-update-ui/06-02-SUMMARY.md`
</output>
