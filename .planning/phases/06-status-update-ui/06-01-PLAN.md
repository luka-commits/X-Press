---
phase: 06-status-update-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/status/StatusButtons.tsx, src/components/status/index.ts, src/app/status/page.tsx]
autonomous: true
---

<objective>
Create 3 large status buttons for mobile status updates with API integration.

Purpose: Enable workers to update order status with 3 clicks from mobile device.
Output: Working StatusButtons component that calls PATCH /api/orders/[id]/status
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Dependencies from prior phases:
@.planning/phases/02-status-api/02-01-SUMMARY.md
@.planning/phases/05-order-selection/05-01-SUMMARY.md

# Source files:
@src/components/status/OrderDetails.tsx
@src/app/status/page.tsx
@src/app/api/orders/[id]/status/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusButtons component</name>
  <files>src/components/status/StatusButtons.tsx, src/components/status/index.ts</files>
  <action>
Create StatusButtons component with 3 large, mobile-friendly buttons:

1. "In Produktion" (in_produktion) - Blue accent color
2. "Fertig" (fertig) - Green/success color
3. "Problem" (problem) - Red/warning color

Requirements:
- Large touch targets: min-h-14 (56px) for mobile usability
- Full width buttons stacked vertically with gap-3
- Color coding: use Tailwind colors (blue-500, green-500, red-500 with hover states)
- Props: orderId (string), onStatusChange callback, disabled state, loading state
- Show loading spinner on the button being pressed while API call in progress
- disabled prop to prevent clicks during loading

Use existing patterns from OrderDetails for styling consistency (rounded-lg, ghl-border, etc.)
  </action>
  <verify>npm run build succeeds, TypeScript compiles without errors</verify>
  <done>StatusButtons component created with 3 styled buttons, loading state, and typed props</done>
</task>

<task type="auto">
  <name>Task 2: Integrate StatusButtons with API</name>
  <files>src/app/status/page.tsx</files>
  <action>
Integrate StatusButtons into status page:

1. Add StatusButtons below OrderDetails when an order is selected
2. Implement handleStatusChange function that:
   - Calls PATCH /api/orders/{orderId}/status with { istStatus }
   - Shows loading state during request
   - On success: log result (Plan 06-02 adds proper feedback)
   - On error: console.error for now (Plan 06-02 adds proper feedback)
3. Pass selectedOrder.auftragsnummer as orderId to StatusButtons

API call pattern:
```typescript
const response = await fetch(`/api/orders/${orderId}/status`, {
  method: 'PATCH',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ istStatus }),
});
```

Keep the existing order selection flow intact - status buttons appear after order is selected.
  </action>
  <verify>
1. npm run dev - visit /status
2. Search and select an order
3. Three status buttons appear below order details
4. Click a button - see loading state
5. Check Network tab - PATCH request sent with correct payload
6. Order status updates in database (check via Prisma Studio or API)
  </verify>
  <done>StatusButtons integrated, API calls work, loading states show during update</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] npm run lint passes
- [ ] Status buttons render below OrderDetails
- [ ] All 3 buttons are clickable and show loading state
- [ ] PATCH requests sent to /api/orders/[id]/status
- [ ] Order status updates in database
</verification>

<success_criteria>
- StatusButtons component created with proper typing
- 3 buttons with distinct colors for each status
- Mobile-friendly touch targets (min-h-14)
- API integration working end-to-end
- Loading state prevents double-clicks
</success_criteria>

<output>
After completion, create `.planning/phases/06-status-update-ui/06-01-SUMMARY.md`
</output>
