---
phase: 32-component-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/orders/__tests__/OrderFilters.test.tsx
  - src/components/versand/__tests__/VersandKPIs.test.tsx
autonomous: true
---

<objective>
Test Orders filter state management and Versand KPI calculations with comprehensive coverage for URL parameter handling, filter interactions, and status counting logic.

Purpose: Ensure OrderFilters correctly updates URL params on filter changes and VersandKPIs accurately counts orders by status.
Output: 2 test files with ~25 test cases covering OrderFilters and VersandKPIs components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@src/components/orders/OrderFilters.tsx
@src/components/versand/VersandKPIs.tsx
@src/components/versand/VersandOrderCard.tsx
@src/components/dashboard/__tests__/KPICard.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test OrderFilters component</name>
  <files>src/components/orders/__tests__/OrderFilters.test.tsx</files>
  <action>
Create comprehensive tests for OrderFilters covering:

1. **Initial render:**
   - Renders deadline dropdown with "Alle Termine" default
   - Renders pipeline dropdown with "Alle Status" default
   - Renders produkttyp dropdown when produkttypen prop not empty
   - Renders sachbearbeiter dropdown when sachbearbeiter prop not empty
   - Does NOT show "Filter zurücksetzen" when no filters active

2. **URL param reading:**
   - Reads current filter values from searchParams
   - Shows correct selected value in dropdowns based on URL

3. **Filter changes:**
   - Selecting deadline filter calls router.push with updated params
   - Selecting pipeline filter calls router.push with updated params
   - Selecting produkttyp updates URL correctly
   - Selecting sachbearbeiter updates URL correctly
   - Resets page to 1 when filter changes

4. **Clear filters:**
   - Shows "Filter zurücksetzen" button when any filter is active
   - Clicking clear removes all filters except search
   - Preserves search param when clearing filters

Mock next/navigation's useRouter and useSearchParams.
Create helper to simulate searchParams state.
  </action>
  <verify>npm test -- OrderFilters.test.tsx --verbose</verify>
  <done>All OrderFilters tests pass, covering initial render, URL reading, filter changes, and clear functionality</done>
</task>

<task type="auto">
  <name>Task 2: Test VersandKPIs component</name>
  <files>src/components/versand/__tests__/VersandKPIs.test.tsx</files>
  <action>
Create comprehensive tests for VersandKPIs covering:

1. **Count calculations:**
   - Counts offen orders (versandStatus === 'offen' or null)
   - Counts versandbereit orders
   - Counts versendet orders
   - Counts ueberfaellig orders (liefertermin < today AND not versendet)

2. **Rendering:**
   - Renders 4 KPI cards (Offen, Versandbereit, Versendet, Überfällig)
   - Shows correct count values in each card
   - Shows correct icons for each card

3. **Variant styles:**
   - Default variant for Offen
   - Amber variant for Versandbereit
   - Green variant for Versendet
   - Red variant for Überfällig when count > 0
   - Default variant for Überfällig when count === 0

4. **Click handling:**
   - Clicking Offen card calls onFilterClick('offen')
   - Clicking Versandbereit card calls onFilterClick('versandbereit')
   - Clicking Versendet card calls onFilterClick('all')
   - Überfällig card is not clickable (no onClick)

5. **Active state:**
   - Shows active styles (border-blue-400) when activeFilter matches

Create mock VersandOrder data matching the interface from VersandOrderCard.tsx.
Use jest.fn() for onFilterClick callback verification.
  </action>
  <verify>npm test -- VersandKPIs.test.tsx --verbose</verify>
  <done>All VersandKPIs tests pass, covering count calculations, rendering, variants, and click handling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes all new tests
- [ ] No TypeScript errors
- [ ] Tests follow project patterns (co-located __tests__ directory)
- [ ] Mock patterns consistent with existing tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- ~25 new test cases added
- OrderFilters URL handling tested comprehensively
- VersandKPIs calculations verified with edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/32-component-tests/32-02-SUMMARY.md`
</output>
