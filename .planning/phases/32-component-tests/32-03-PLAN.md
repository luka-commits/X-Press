---
phase: 32-component-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reports/__tests__/SnapshotKPIs.test.tsx
  - src/components/reports/__tests__/ReportsOrdersDialog.test.tsx
autonomous: true
---

<objective>
Test Reports KPI display and dialog components with comprehensive coverage for loading states, click interactions, keyboard accessibility, and data display.

Purpose: Ensure Reports components correctly display KPI data, handle loading states, and provide accessible interactions.
Output: 2 test files with ~25 test cases covering SnapshotKPIs and ReportsOrdersDialog components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@src/components/reports/SnapshotKPIs.tsx
@src/components/reports/ReportsOrdersDialog.tsx
@src/components/dashboard/__tests__/KPICard.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test SnapshotKPIs component</name>
  <files>src/components/reports/__tests__/SnapshotKPIs.test.tsx</files>
  <action>
Create comprehensive tests for SnapshotKPIs covering:

1. **Loading state:**
   - Shows loading spinner (Loader2) when loading=true
   - Shows "Lade..." text in each card

2. **Data display:**
   - Shows "Aktueller Stand" header
   - Renders 4 KPI cards in grid
   - Displays aktiveAuftraege value
   - Displays problemAuftraege value
   - Displays aeltesterAuftrag with "Tage" suffix
   - Shows "-" for aeltesterAuftrag when null
   - Displays morgenFaellig value

3. **Highlight conditions:**
   - problemAuftraege highlighted (text-red-600) when > 0
   - aeltesterAuftrag highlighted (text-amber-600) when > 7
   - morgenFaellig highlighted (text-amber-600) when > 0
   - No highlight when conditions not met

4. **Click handling:**
   - Clicking problemAuftraege calls onKpiClick('problem')
   - Clicking aeltesterAuftrag calls onKpiClick('oldest')
   - Clicking morgenFaellig calls onKpiClick('tomorrow')
   - Clicking aktiveAuftraege calls onKpiClick('openOrders')

5. **Keyboard accessibility:**
   - Cards with onClick have role="button" and tabIndex=0
   - Enter key triggers click handler
   - Space key triggers click handler

Use jest.fn() for onKpiClick callback verification.
Test with sample data objects matching the props interface.
  </action>
  <verify>npm test -- SnapshotKPIs.test.tsx --verbose</verify>
  <done>All SnapshotKPIs tests pass, covering loading, display, highlights, clicks, and keyboard a11y</done>
</task>

<task type="auto">
  <name>Task 2: Test ReportsOrdersDialog component</name>
  <files>src/components/reports/__tests__/ReportsOrdersDialog.test.tsx</files>
  <action>
Create comprehensive tests for ReportsOrdersDialog covering:

1. **Rendering states:**
   - Shows loading state ("Lädt...") when isOpen changes to true
   - Shows error message when fetch fails
   - Shows empty state ("Keine Aufträge") when no orders returned
   - Renders order table when orders exist

2. **Fetch behavior:**
   - Fetches from /api/reports/kpi-orders with type parameter
   - Adds stage parameter when kpiType='stage' and stage provided
   - Does NOT fetch when dialog is closed
   - Re-fetches when dialog opens again

3. **Order display:**
   - Displays auftragsnummer as link to order detail
   - Displays kunde, produkttyp, liefertermin, tageUebrig
   - Shows "–" for null produkttyp/liefertermin
   - Formats liefertermin as dd.MM.yyyy

4. **tageUebrig badge colors:**
   - Red (bg-capacity-red) for <= 0 days
   - Yellow (bg-capacity-yellow) for 1 day
   - Green (bg-capacity-green) for 2+ days

5. **Dialog title:**
   - Shows title prop in DialogTitle

Mock fetch globally using jest.spyOn(global, 'fetch'). Reset mocks in beforeEach.
Test different kpiType values: 'problem', 'oldest', 'tomorrow', 'stage'.
  </action>
  <verify>npm test -- ReportsOrdersDialog.test.tsx --verbose</verify>
  <done>All ReportsOrdersDialog tests pass, covering loading/error/empty/data states, fetch params, and badge colors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes all new tests
- [ ] No TypeScript errors
- [ ] Tests follow project patterns (co-located __tests__ directory)
- [ ] Keyboard accessibility properly tested
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- ~25 new test cases added
- Reports components have comprehensive test coverage
- Accessibility patterns verified
</success_criteria>

<output>
After completion, create `.planning/phases/32-component-tests/32-03-SUMMARY.md`
</output>
