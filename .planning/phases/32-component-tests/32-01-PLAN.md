---
phase: 32-component-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/__tests__/KPIOrdersDialog.test.tsx
  - src/components/dashboard/__tests__/DashboardClient.test.tsx
autonomous: true
---

<objective>
Test Dashboard dialog and client components with comprehensive coverage for loading states, error handling, data display, and user interactions.

Purpose: Ensure Dashboard dialog components handle async data fetching correctly and provide proper user feedback.
Output: 2 test files with ~20 test cases covering KPIOrdersDialog and DashboardClient components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@src/components/dashboard/KPIOrdersDialog.tsx
@src/components/dashboard/DashboardClient.tsx
@src/hooks/useDashboardRefresh.ts
@src/components/dashboard/__tests__/KPICard.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test KPIOrdersDialog component</name>
  <files>src/components/dashboard/__tests__/KPIOrdersDialog.test.tsx</files>
  <action>
Create comprehensive tests for KPIOrdersDialog covering:

1. **Rendering states:**
   - Shows loading state when isOpen changes to true
   - Shows error message when fetch fails
   - Shows empty state when no orders returned
   - Renders order table when orders exist

2. **Fetch behavior:**
   - Fetches from correct URL with kpiType parameter
   - Does NOT fetch when dialog is closed
   - Re-fetches when dialog opens again

3. **Order display:**
   - Displays auftragsnummer, kunde, produkttyp, liefertermin, tageUebrig
   - Link navigates to order detail page
   - Shows "–" for null produkttyp/liefertermin
   - Formats liefertermin as dd.MM.yyyy

4. **tageUebrig badge colors:**
   - Red (bg-capacity-red) for 0 days
   - Yellow (bg-capacity-yellow) for 1 day
   - Green (bg-capacity-green) for 2+ days

Mock fetch globally using jest.spyOn(global, 'fetch'). Reset mocks in beforeEach.
Follow existing KPICard.test.tsx patterns for structure.
  </action>
  <verify>npm test -- KPIOrdersDialog.test.tsx --verbose</verify>
  <done>All KPIOrdersDialog tests pass, covering loading/error/empty/data states and badge colors</done>
</task>

<task type="auto">
  <name>Task 2: Test DashboardClient component</name>
  <files>src/components/dashboard/__tests__/DashboardClient.test.tsx</files>
  <action>
Create tests for DashboardClient covering:

1. **Rendering:**
   - Shows lastUpdated time
   - Shows countdown text
   - Shows "Aktualisieren" button

2. **Hook mocking:**
   - Mock useDashboardRefresh hook to control state
   - Test different states: isRefreshing=true shows spinner, showSuccess=true shows checkmark

3. **Button states:**
   - Default: Shows "Aktualisieren" with RefreshCw icon
   - Refreshing: Button disabled, icon animates (has animate-spin class)
   - Success: Shows "Aktualisiert" with Check icon, green background

4. **Click handling:**
   - Click calls handleRefresh from hook

Mock the useDashboardRefresh hook using jest.mock('@/hooks'). Return controlled state objects.
  </action>
  <verify>npm test -- DashboardClient.test.tsx --verbose</verify>
  <done>All DashboardClient tests pass, covering render states and button interactions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test passes all new tests
- [ ] No TypeScript errors
- [ ] Tests follow project patterns (co-located __tests__ directory)
- [ ] Mocking patterns consistent with existing tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- ~20 new test cases added
- Dashboard components have comprehensive test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/32-component-tests/32-01-SUMMARY.md`
</output>
