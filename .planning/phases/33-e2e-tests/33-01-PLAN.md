---
phase: 33-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, playwright.config.ts, e2e/smoke.spec.ts]
autonomous: true
---

<objective>
Set up Playwright E2E testing framework with smoke tests for all pages.

Purpose: Establish E2E testing infrastructure for workflow testing.
Output: Playwright configured, npm scripts added, basic smoke tests passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

# Test infrastructure from phase 30
@.planning/phases/30-test-setup/30-01-SUMMARY.md

# Package.json for scripts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and configure for Next.js</name>
  <files>package.json, playwright.config.ts</files>
  <action>
    1. Run `npm init playwright@latest` with options:
       - TypeScript: yes
       - Tests folder: e2e
       - GitHub Actions: no
       - Install browsers: yes
    2. After installation, update playwright.config.ts:
       - Set baseURL to http://localhost:3000
       - Set webServer config to run `npm run dev` on port 3000
       - Set reuseExistingServer to true (use running dev server if exists)
       - Set testDir to './e2e'
       - Keep only chromium in projects (remove webkit/firefox for faster runs)
    3. Add npm scripts to package.json:
       - "test:e2e": "playwright test"
       - "test:e2e:ui": "playwright test --ui"
       - "test:e2e:debug": "playwright test --debug"
  </action>
  <verify>npm run test:e2e -- --version outputs Playwright version</verify>
  <done>Playwright installed, config created, npm scripts added</done>
</task>

<task type="auto">
  <name>Task 2: Create smoke tests for all pages</name>
  <files>e2e/smoke.spec.ts</files>
  <action>
    Create e2e/smoke.spec.ts with smoke tests that verify pages load:

    1. Dashboard (/) - loads, has title
    2. Status (/status) - loads, has search input
    3. Orders (/orders) - loads, has order list or table
    4. Versand (/versand) - loads, has map or list
    5. Reports (/reports) - loads, has KPIs or tabs

    Use Playwright's page.goto() and expect(page).toHaveTitle() or
    expect(page.locator(...)).toBeVisible() patterns.

    Each test should have descriptive name and timeout of 10s max.
    Group tests in describe('Smoke Tests', ...).
  </action>
  <verify>npm run test:e2e passes with all smoke tests green</verify>
  <done>5 smoke tests passing, all main pages verified to load</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:e2e` passes
- [ ] playwright.config.ts has correct baseURL and webServer config
- [ ] All 5 pages verified to load (/,/status,/orders,/versand,/reports)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Playwright installed with chromium browser
- E2E smoke tests passing for all main pages
  </success_criteria>

<output>
After completion, create `.planning/phases/33-e2e-tests/33-01-SUMMARY.md`
</output>
