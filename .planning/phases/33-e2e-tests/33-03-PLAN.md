---
phase: 33-e2e-tests
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified: [e2e/dashboard-drilldown.spec.ts, e2e/reports-drilldown.spec.ts]
autonomous: true
---

<objective>
E2E tests for Dashboard and Reports KPI drilldown functionality.

Purpose: Verify KPI click → dialog workflow on Dashboard and Reports pages.
Output: E2E tests covering KPI interaction patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Setup summary for patterns
@.planning/phases/33-e2e-tests/33-01-SUMMARY.md

# Dashboard page
@src/app/page.tsx

# Dashboard client component (has KPI cards)
@src/components/dashboard/DashboardClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Dashboard KPI drilldown</name>
  <files>e2e/dashboard-drilldown.spec.ts</files>
  <action>
    Create e2e/dashboard-drilldown.spec.ts with tests:

    1. Dashboard KPI cards:
       - Dashboard loads with KPI cards visible
       - Cards show counts (numbers)
       - Cards are clickable (cursor pointer or role button)

    2. KPI drilldown dialog:
       - Click on KPI card → dialog opens
       - Dialog has title matching KPI category
       - Dialog shows order list or "Keine Aufträge" message
       - Dialog has close button (X)
       - Clicking close button dismisses dialog
       - Clicking outside dialog dismisses it (optional)

    3. Multiple KPIs:
       - Test "Offen" KPI click → dialog with open orders
       - Test "In Produktion" KPI click → dialog with production orders
       - Test "Fertig" KPI click → dialog with finished orders
       - Test "Heute fällig" KPI click → dialog with due-today orders

    Use page.getByRole('dialog') to verify dialog opened.
    Use page.getByTestId() if KPI cards have data-testid attributes.
    Use locator.click() with {force: true} if needed for overlay issues.
  </action>
  <verify>npm run test:e2e -- dashboard-drilldown passes</verify>
  <done>Dashboard KPI drilldown tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Test Reports page and KPI drilldown</name>
  <files>e2e/reports-drilldown.spec.ts</files>
  <action>
    Create e2e/reports-drilldown.spec.ts with tests:

    1. Reports page structure:
       - Reports page loads
       - SnapshotKPIs visible with Pipeline stats
       - PipelineFunnel chart visible
       - ThroughputChart visible

    2. Reports KPI drilldown:
       - Click on SnapshotKPI → dialog opens
       - Dialog shows orders matching the category
       - Dialog can be closed

    3. Reports navigation (if tabs exist):
       - Verify tabs or navigation elements
       - Test tab switching if applicable

    4. Date range picker (if visible):
       - DateRangePicker component exists
       - Clicking opens popover (optional depth)

    Note: Reports page may have different KPI layout than Dashboard.
    Use locators that match the actual SnapshotKPIs component structure.

    Keep tests focused on critical drilldown path - don't over-test charting.
  </action>
  <verify>npm run test:e2e -- reports-drilldown passes</verify>
  <done>Reports drilldown tests passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:e2e -- dashboard-drilldown` passes
- [ ] `npm run test:e2e -- reports-drilldown` passes
- [ ] Dashboard KPI click → dialog workflow verified
- [ ] Reports KPI click → dialog workflow verified
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dashboard drilldown E2E tests comprehensive
- Reports drilldown E2E tests comprehensive
- Dialog open/close behavior verified
  </success_criteria>

<output>
After completion, create `.planning/phases/33-e2e-tests/33-03-SUMMARY.md`
</output>
