---
phase: 33-e2e-tests
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified: [e2e/status-workflow.spec.ts]
autonomous: true
---

<objective>
E2E tests for the mobile status-update 3-click workflow.

Purpose: Verify the core user journey - search order, select, update status.
Output: Comprehensive E2E tests for /status page workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Setup summary for patterns
@.planning/phases/33-e2e-tests/33-01-SUMMARY.md

# Status page component
@src/app/status/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test status page search and selection workflow</name>
  <files>e2e/status-workflow.spec.ts</files>
  <action>
    Create e2e/status-workflow.spec.ts with tests for:

    1. Page structure:
       - Status page loads with search input visible
       - Search input has proper placeholder text

    2. Search behavior (use real data if available, or mock patterns):
       - Type in search input triggers search
       - Results appear below search
       - Clicking result selects the order
       - Order details card appears after selection

    3. Order selection:
       - Order details shows Auftragsnummer, Kunde, Produkt
       - Clear button visible and functional
       - Clicking clear returns to initial state

    Use Playwright locators: page.getByPlaceholder(), page.getByText(),
    page.getByRole(), page.locator('[data-testid="..."]').

    Note: If no orders exist in dev database, tests should verify UI states
    (empty search, no results message) rather than actual data.
  </action>
  <verify>npm run test:e2e -- status-workflow passes</verify>
  <done>Search and selection workflow tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Test status update buttons and feedback</name>
  <files>e2e/status-workflow.spec.ts</files>
  <action>
    Add tests to e2e/status-workflow.spec.ts for status update flow:

    1. Status buttons:
       - After order selection, 3 status buttons visible (In Produktion, Fertig, Problem)
       - Buttons have correct colors (amber, green, red)
       - Buttons are clickable

    2. Status update (if database has test data):
       - Click "In Produktion" → success feedback appears
       - Click "Fertig" → success feedback appears
       - Click "Problem" → prompt for comment appears

    3. Feedback:
       - Success feedback shows "Status aktualisiert"
       - Feedback auto-dismisses after ~3 seconds
       - Error feedback shows error message (test with network mock if possible)

    Use page.waitForSelector() for async UI updates.
    Use expect(locator).toHaveClass() for color verification.

    Note: These tests depend on database state. If no test data available,
    verify UI components exist and are interactive without asserting API results.
  </action>
  <verify>npm run test:e2e -- status-workflow passes all tests</verify>
  <done>Status button and feedback tests passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:e2e -- status-workflow` passes
- [ ] Search workflow tested (input, results, selection)
- [ ] Status buttons tested (visibility, colors, interaction)
- [ ] Feedback tested (success message appearance)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Status workflow E2E tests comprehensive
- Tests handle both data-present and empty-database scenarios
  </success_criteria>

<output>
After completion, create `.planning/phases/33-e2e-tests/33-02-SUMMARY.md`
</output>
