---
phase: 12-kartenansicht
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - package.json
  - src/components/map/DeliveryMap.tsx
  - src/components/map/MapContainer.tsx
  - src/components/map/index.ts
  - src/app/versand/page.tsx
autonomous: false
---

<objective>
Create map component with PLZ clustering and integrate into /versand page.

Purpose: Visualize delivery addresses on interactive map with clustered markers for route optimization.
Output: Working map on /versand page showing order locations grouped by PLZ proximity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-kartenansicht/12-RESEARCH.md
@.planning/phases/12-kartenansicht/12-01-SUMMARY.md

@src/app/versand/page.tsx
@src/components/versand/VersandOrderList.tsx
@src/components/versand/VersandOrderCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install map dependencies</name>
  <files>package.json</files>
  <action>
Install required packages for Leaflet map with clustering:

npm install leaflet react-leaflet react-leaflet-cluster leaflet-defaulticon-compatibility @types/leaflet

Packages per 12-RESEARCH.md standard stack:
- leaflet 1.9.x: Map rendering (42kB, free OSM tiles)
- react-leaflet 5.0.x: React bindings
- react-leaflet-cluster 3.x: Marker clustering for PLZ groups
- leaflet-defaulticon-compatibility: Fix marker icons in Next.js/webpack
- @types/leaflet: TypeScript definitions
  </action>
  <verify>npm ls leaflet react-leaflet react-leaflet-cluster shows all packages installed</verify>
  <done>All map dependencies installed and in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create map components with clustering</name>
  <files>src/components/map/DeliveryMap.tsx, src/components/map/MapContainer.tsx, src/components/map/index.ts</files>
  <action>
Create src/components/map/ directory with:

1. DeliveryMap.tsx ("use client"):
   - Import Leaflet CSS, MarkerCluster CSS, leaflet-defaulticon-compatibility CSS (CRITICAL - order matters)
   - Import MapContainer, TileLayer, Marker, Popup from react-leaflet
   - Import MarkerClusterGroup from react-leaflet-cluster
   - Accept orders prop: Array<{auftragsnummer, kunde, lieferPlz, lieferOrt, lieferLat, lieferLng}>
   - Filter orders to only those with lat/lng
   - Render MapContainer centered on Berlin [52.52, 13.405], zoom 10
   - Use OpenStreetMap tiles (free, no API key)
   - Wrap markers in MarkerClusterGroup with chunkedLoading prop
   - Each marker shows popup with auftragsnummer, kunde, PLZ/Ort

2. MapContainer.tsx (dynamic import wrapper):
   - Use next/dynamic with ssr: false (CRITICAL for Next.js - Leaflet needs window)
   - Loading fallback: div with h-[400px] bg-muted animate-pulse "Karte wird geladen..."
   - Export named DeliveryMap

3. index.ts: Barrel export for DeliveryMap

Follow patterns exactly from 12-RESEARCH.md code_examples section.
  </action>
  <verify>npm run build succeeds (checks TypeScript and dynamic import)</verify>
  <done>Map components created with clustering, SSR-compatible dynamic import</done>
</task>

<task type="auto">
  <name>Task 3: Integrate map into /versand page</name>
  <files>src/app/versand/page.tsx, src/components/versand/VersandOrderList.tsx</files>
  <action>
Option A (simpler): Add map directly to VersandOrderList component
- Import DeliveryMap from @/components/map
- Add map section above the order list
- Pass orders array (which already has coordinates from Plan 01)
- Map height: h-[300px] or h-[400px] on mobile

Layout:
- Map at top (collapsed by default with "Karte anzeigen" toggle, or always visible)
- Existing filter pills below
- Order list below filters

Add a simple toggle state (showMap) with a button to show/hide map for mobile performance.
When showMap is true, render DeliveryMap with current orders.
  </action>
  <verify>npm run dev, visit localhost:3000/versand, map renders with markers</verify>
  <done>Map integrated into versand page, shows order locations with clustering</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive map on /versand page showing delivery addresses with PLZ clustering</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/versand
    3. Click "Karte anzeigen" toggle (if implemented) or scroll to see map
    4. Verify: Map renders centered on Berlin area
    5. Verify: Markers appear for orders with addresses
    6. Verify: Markers cluster when zoomed out (numbers in circles)
    7. Verify: Clicking a marker shows popup with order info
    8. Verify: Clicking a cluster zooms in to show individual markers
    9. Mobile: Map is responsive, touch controls work
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Map dependencies installed
- [ ] `npm run build` succeeds
- [ ] Map renders on /versand page
- [ ] Markers cluster correctly
- [ ] Popups show order information
- [ ] Human verification approved
</verification>

<success_criteria>

- All tasks completed
- Map visualization working
- PLZ clustering groups nearby orders
- User verified map functionality
</success_criteria>

<output>
After completion, create `.planning/phases/12-kartenansicht/12-02-SUMMARY.md`
</output>
