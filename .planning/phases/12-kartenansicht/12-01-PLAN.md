---
phase: 12-kartenansicht
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/geocoding/plz-lookup.ts
  - src/lib/geocoding/plz-data.json
  - src/components/versand/VersandOrderCard.tsx
  - src/app/api/versand/orders/route.ts
autonomous: true
---

<objective>
Add coordinate fields to database and create PLZ geocoding lookup for map visualization.

Purpose: Enable map markers by providing lat/lng coordinates for delivery addresses based on PLZ.
Output: Database schema with lieferLat/lieferLng, PLZ lookup utility, API returning coordinates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-kartenansicht/12-RESEARCH.md

@prisma/schema.prisma
@src/components/versand/VersandOrderCard.tsx
@src/app/api/versand/orders/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coordinate fields to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>
Add lieferLat and lieferLng fields to the Auftrag model after the existing lieferLand field:
- lieferLat: Float? (latitude for map marker)
- lieferLng: Float? (longitude for map marker)

Run `npx prisma db push` to sync schema (not migrate - project uses db push pattern per KEY DECISIONS).
  </action>
  <verify>npx prisma db push succeeds without errors</verify>
  <done>Auftrag model has lieferLat and lieferLng Float? fields in database</done>
</task>

<task type="auto">
  <name>Task 2: Create PLZ lookup utility with static dataset</name>
  <files>src/lib/geocoding/plz-lookup.ts, src/lib/geocoding/plz-data.json</files>
  <action>
1. Download WZB PLZ geocoord dataset (https://github.com/WZBSocialScienceCenter/plz_geocoord/raw/main/plz_geocoord.csv)
2. Convert CSV to JSON array format: [{"plz": "10115", "lat": 52.532, "lng": 13.388}, ...]
3. Save as src/lib/geocoding/plz-data.json (~200KB, 8000+ entries)
4. Create src/lib/geocoding/plz-lookup.ts:
   - Import plz-data.json
   - Build Map<string, {lat: number, lng: number}> on module load
   - Export getCoordinatesForPlz(plz: string): {lat: number, lng: number} | null
   - Normalize PLZ to 5 digits with padStart(5, '0')

Pattern from 12-RESEARCH.md code examples.
  </action>
  <verify>Create a test: node -e "const {getCoordinatesForPlz} = require('./src/lib/geocoding/plz-lookup'); console.log(getCoordinatesForPlz('10115'))" returns Berlin coordinates</verify>
  <done>PLZ lookup returns coordinates for valid German PLZ, null for invalid</done>
</task>

<task type="auto">
  <name>Task 3: Update VersandOrder type and API to include coordinates</name>
  <files>src/components/versand/VersandOrderCard.tsx, src/app/api/versand/orders/route.ts</files>
  <action>
1. Update VersandOrder interface in VersandOrderCard.tsx:
   - Add lieferLat: number | null
   - Add lieferLng: number | null

2. Update GET /api/versand/orders route:
   - Import getCoordinatesForPlz from @/lib/geocoding/plz-lookup
   - In the Prisma select, add lieferLat and lieferLng fields
   - For each order in response, if lieferLat/lieferLng are null but lieferPlz exists:
     - Look up coordinates using getCoordinatesForPlz(lieferPlz)
     - Return looked-up coordinates (don't write to DB in GET - read-only)
   - This provides coordinates even for orders not yet geocoded in DB
  </action>
  <verify>curl localhost:3000/api/versand/orders returns orders with lieferLat/lieferLng populated for orders with lieferPlz</verify>
  <done>API returns coordinates for orders, VersandOrder type includes lat/lng</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma db push` succeeds
- [ ] plz-lookup.ts exports working getCoordinatesForPlz function
- [ ] GET /api/versand/orders returns lieferLat/lieferLng for orders with PLZ
- [ ] `npm run build` succeeds without TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- Database has coordinate fields
- PLZ lookup utility works for German postal codes
- API returns coordinates for map visualization
</success_criteria>

<output>
After completion, create `.planning/phases/12-kartenansicht/12-01-SUMMARY.md`
</output>
