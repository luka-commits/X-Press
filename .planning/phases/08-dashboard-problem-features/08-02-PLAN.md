---
phase: 08-dashboard-problem-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/orders/OrderFilters.tsx, src/app/orders/page.tsx]
autonomous: true
---

<objective>
Add IST-Status filter to OrderFilters allowing users to filter for problem orders (istStatus='problem').

Purpose: Enable managers to quickly find and address orders with production problems.
Output: New "IST-Status" dropdown filter in orders list with options: Alle, Fertig, In Produktion, Problem.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-status-column/07-01-SUMMARY.md

@src/components/orders/OrderFilters.tsx
@src/app/orders/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add istStatus filter to OrderFilters component</name>
  <files>src/components/orders/OrderFilters.tsx</files>
  <action>
Add new IST-Status dropdown filter following existing pattern (like statusOptions):

1. Add istStatus to searchParams reading:
   ```typescript
   const currentIstStatus = searchParams.get('istStatus') || 'all';
   ```

2. Add istStatusOptions array:
   ```typescript
   const istStatusOptions = [
     { value: 'all', label: 'Alle IST-Status' },
     { value: 'fertig', label: 'Fertig' },
     { value: 'in_produktion', label: 'In Produktion' },
     { value: 'problem', label: 'Problem' },
   ];
   ```

3. Add Select component after Status dropdown (before Produkttyp):
   ```tsx
   <Select
     value={currentIstStatus}
     onValueChange={(value) => updateFilter('istStatus', value)}
   >
     <SelectTrigger className="w-[150px] bg-white">
       <SelectValue placeholder="IST-Status" />
     </SelectTrigger>
     <SelectContent>
       {istStatusOptions.map((option) => (
         <SelectItem key={option.value} value={option.value}>
           {option.label}
         </SelectItem>
       ))}
     </SelectContent>
   </Select>
   ```

4. Update hasActiveFilters to include currentIstStatus !== 'all'
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>IST-Status dropdown appears in OrderFilters with all options</done>
</task>

<task type="auto">
  <name>Task 2: Apply istStatus filter in orders query</name>
  <files>src/app/orders/page.tsx</files>
  <action>
1. Add istStatus to searchParams interface:
   ```typescript
   istStatus?: string;
   ```

2. In getOrders function, add istStatus parameter extraction:
   ```typescript
   const istStatus = params.istStatus || 'all';
   ```

3. Add istStatus filter to query (after sachbearbeiter filter):
   ```typescript
   // IST-Status filter
   if (istStatus && istStatus !== 'all') {
     query = query.eq('istStatus', istStatus);
   }
   ```

This follows the exact same pattern as the existing produkttyp and sachbearbeiter filters.
  </action>
  <verify>
1. `npm run build` succeeds
2. Visit http://localhost:3000/orders
3. Select "Problem" from IST-Status dropdown
4. Verify only orders with istStatus='problem' are shown (or empty if none)
5. Select "Alle IST-Status" - all orders shown again
  </verify>
  <done>IST-Status filter works, filtering orders by istStatus value</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Orders page loads without errors
- [ ] IST-Status dropdown visible in filter row
- [ ] Selecting "Problem" shows only problem orders
- [ ] Selecting "Fertig" shows only completed orders
- [ ] Selecting "In Produktion" shows only in-production orders
- [ ] "Filter zurücksetzen" clears IST-Status filter
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Filter works correctly with URL parameters (?istStatus=problem)
- Filter combines correctly with other filters (search, status, deadline)
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-problem-features/08-02-SUMMARY.md`
</output>
