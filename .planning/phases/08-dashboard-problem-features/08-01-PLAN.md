---
phase: 08-dashboard-problem-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/dashboard-queries.ts, src/components/dashboard/KPICard.tsx, src/app/page.tsx]
autonomous: true
---

<objective>
Add Problem-Zähler KPI card to the dashboard showing count of orders with istStatus='problem'.

Purpose: Give managers immediate visibility into production problems requiring attention.
Output: New "Problem-Aufträge" KPI card on dashboard homepage with red styling when count > 0.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-status-column/07-01-SUMMARY.md

@src/lib/dashboard-queries.ts
@src/components/dashboard/KPICard.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getProblemOrdersCount query function</name>
  <files>src/lib/dashboard-queries.ts</files>
  <action>
Add a new query function `getProblemOrdersCount()` that:
1. Queries Auftrag table for count where istStatus='problem' AND status='aktiv'
2. Returns the count as number
3. Add to DashboardKPIs interface: `problemOrders: number`
4. Update getDashboardKPIs() to include problemOrders count in the parallel queries

Pattern follows existing criticalResult query style:
```typescript
// Problem orders (istStatus = 'problem')
supabase
  .from('Auftrag')
  .select('*', { count: 'exact', head: true })
  .eq('status', 'aktiv')
  .eq('istStatus', 'problem')
```
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>getProblemOrdersCount returns count, DashboardKPIs includes problemOrders field</done>
</task>

<task type="auto">
  <name>Task 2: Add Problem-Aufträge KPI card to dashboard</name>
  <files>src/components/dashboard/KPICard.tsx, src/app/page.tsx</files>
  <action>
1. In KPICardsGrid (in KPICard.tsx):
   - Add `problemOrders: number` to KPICardsGridProps interface
   - Add new KPICard after "Bald fällig" card:
     ```tsx
     <KPICard
       label="Problem-Aufträge"
       value={problemOrders}
       variant={problemOrders > 0 ? 'critical' : 'default'}
     />
     ```
   - Grid already handles 4+ cards with responsive layout

2. In src/app/page.tsx:
   - Pass `problemOrders={kpis.problemOrders}` to KPICardsGrid component

The critical variant uses text-capacity-red (red color) when count > 0.
  </action>
  <verify>
1. `npm run build` succeeds
2. Visit http://localhost:3000 - Problem-Aufträge card visible in KPI row
3. Card shows red text if any problem orders exist
  </verify>
  <done>Problem-Aufträge KPI card visible on dashboard, red when count > 0</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Dashboard page loads without errors
- [ ] Problem-Aufträge KPI card displays count from database
- [ ] Card uses red text (critical variant) when problemOrders > 0
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Problem count matches database query: SELECT COUNT(*) FROM "Auftrag" WHERE "istStatus"='problem' AND status='aktiv'
- Visual styling consistent with existing KPI cards
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-problem-features/08-01-SUMMARY.md`
</output>
