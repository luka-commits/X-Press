---
phase: 11-versand-ui-seite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/versand/page.tsx
  - src/components/versand/VersandOrderCard.tsx
  - src/components/versand/VersandStatusButtons.tsx
  - src/components/versand/VersandOrderList.tsx
  - src/components/versand/index.ts
autonomous: true
---

<objective>
Create /versand route for shipping team with PLZ-sorted order list and Versandbereit/Versendet status buttons.

Purpose: Enable shipping team to see which orders need shipping today/this week, sorted by PLZ for route optimization, and mark orders as versandbereit or versendet with a single tap.
Output: Working /versand page with order list, deadline filters, PLZ sorting, and status update buttons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versand-datenmodell/09-01-SUMMARY.md
@.planning/phases/10-versand-api-liste/10-01-SUMMARY.md

# Existing patterns to follow:
@src/app/status/page.tsx
@src/components/status/StatusButtons.tsx
@src/components/layout/MobileLayout.tsx

# API endpoints to consume:
@src/app/api/versand/orders/route.ts
@src/app/api/orders/[id]/versand/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VersandOrderCard and VersandStatusButtons components</name>
  <files>src/components/versand/VersandOrderCard.tsx, src/components/versand/VersandStatusButtons.tsx, src/components/versand/index.ts</files>
  <action>
Create two components following existing patterns:

**VersandOrderCard.tsx:**
- Display order info: Auftragsnummer, Kunde (firma/name), Produkttyp
- Display address: lieferStrasse, lieferPlz, lieferOrt prominently (PLZ emphasized for sorting visibility)
- Display Liefertermin formatted as dd.MM.yyyy using date-fns/locale/de
- Show current versandStatus with color-coded badge (offen=gray, versandbereit=amber, versendet=green)
- Use bg-ghl-card, border-ghl-border styling to match existing components
- Include onSelect callback for expanding to status buttons

**VersandStatusButtons.tsx:**
- Two large mobile-friendly buttons (min-h-14): "Versandbereit" (amber) and "Versendet" (green)
- Loading state per button during API call (Loader2 spinner)
- Disabled state during any loading
- Optional comment textarea (simpler than StatusButtons - no required comment)
- Follow StatusButtons.tsx pattern exactly for button styling and state management
- Type: VersandStatusType = "offen" | "versandbereit" | "versendet"

**index.ts:** Barrel exports for both components plus VersandStatusType
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>VersandOrderCard displays order with address, VersandStatusButtons has 2 buttons with loading states</done>
</task>

<task type="auto">
  <name>Task 2: Create VersandOrderList component with filters</name>
  <files>src/components/versand/VersandOrderList.tsx</files>
  <action>
Create VersandOrderList component that fetches and displays orders:

**Data fetching:**
- Fetch from GET /api/versand/orders with query params
- State: orders array, loading boolean, error string, selectedOrder
- Re-fetch when filters change

**Filter controls (horizontal pill buttons, mobile-friendly):**
- Deadline filter: "Heute" | "Diese Woche" | "Alle" (maps to deadline=today|week|all)
- VersandStatus filter: "Alle" | "Offen" | "Versandbereit" (maps to versandStatus param)
- Use rounded-full pill buttons like existing OrderFilters pattern

**Order list:**
- Map orders to VersandOrderCard components
- When card tapped, set selectedOrder and show VersandStatusButtons below that card
- When status updated, re-fetch list to reflect changes
- Show "Keine Aufträge gefunden" when empty
- Show loading skeleton (3 placeholder cards) during fetch

**API call for status update:**
- Call PATCH /api/orders/[id]/versand with { versandStatus, versandKommentar }
- Show success/error feedback inline (green/red banner, auto-dismiss after 3s)
- Clear selection after successful update

Default: deadline=today, versandStatus=offen (show today's open orders)
  </action>
  <verify>Component renders with filters, clicking order shows status buttons, status update works</verify>
  <done>VersandOrderList fetches orders, filters work, status updates reflect immediately</done>
</task>

<task type="auto">
  <name>Task 3: Create /versand page</name>
  <files>src/app/versand/page.tsx</files>
  <action>
Create the /versand page using MobileLayout:

**Structure:**
- 'use client' directive
- Import MobileLayout from @/components/layout
- Import VersandOrderList from @/components/versand
- Set title="Versand" and showBack=true in MobileLayout

**Content:**
- Simple wrapper: MobileLayout contains VersandOrderList
- All state management and logic lives in VersandOrderList component

**No additional complexity:**
- Unlike /status page, /versand shows a list directly (no search required)
- Selection/status update handled within VersandOrderList
  </action>
  <verify>Visit http://localhost:3000/versand, page loads with header "Versand" and order list</verify>
  <done>/versand route accessible, shows order list with filters, status updates work end-to-end</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] npm run lint passes (or only pre-existing warnings)
- [ ] /versand page loads and shows order list
- [ ] Deadline filter (Heute/Diese Woche/Alle) filters correctly
- [ ] VersandStatus filter (Alle/Offen/Versandbereit) filters correctly
- [ ] PLZ sorting visible (orders sorted by PLZ ascending)
- [ ] Tapping order shows Versandbereit/Versendet buttons
- [ ] Status update changes order status and refreshes list
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Shipping team can view orders sorted by PLZ
- Shipping team can filter by deadline (today/week/all)
- Shipping team can mark orders as versandbereit or versendet
- UI follows existing mobile patterns (MobileLayout, large touch targets)
</success_criteria>

<output>
After completion, create `.planning/phases/11-versand-ui-seite/11-01-SUMMARY.md`
</output>
