---
phase: 07-dashboard-status-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/orders/page.tsx
  - src/components/orders/OrderTable.tsx
autonomous: true
---

<objective>
Add IST-Status column to OrderTable with color-coded badges showing shopfloor status.

Purpose: Dashboard users can see the actual production status (IST-Zustand) set by mobile workers directly in the order list.
Output: OrderTable displays IST-Status column with color-coded badges (🟢 Fertig, 🟡 In Produktion, 🔴 Problem).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-schema/01-01-SUMMARY.md

@src/app/orders/page.tsx
@src/components/orders/OrderTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add istStatus to orders query and interface</name>
  <files>src/app/orders/page.tsx, src/components/orders/OrderTable.tsx</files>
  <action>
    1. In `src/app/orders/page.tsx`:
       - Modify the Supabase select in `getOrders()` to include `istStatus` field
       - Add `istStatus` to the mapped order object in `ordersWithStatus`
       - Use direct field access: `istStatus: order.istStatus`

    2. In `src/components/orders/OrderTable.tsx`:
       - Add `istStatus: string | null;` to the `Order` interface
       - IstStatus values are: 'in_produktion', 'fertig', 'problem', or null
  </action>
  <verify>TypeScript compiles without errors: `npx tsc --noEmit`</verify>
  <done>Order interface includes istStatus, query fetches istStatus from database</done>
</task>

<task type="auto">
  <name>Task 2: Add IST-Status column with color-coded badge</name>
  <files>src/components/orders/OrderTable.tsx</files>
  <action>
    1. Add `getIstStatusBadge()` function (similar to existing `getStatusBadge()`):
       - null → grey badge "–" (no status set yet)
       - 'in_produktion' → yellow badge "In Produktion" (bg-amber-100 text-amber-700)
       - 'fertig' → green badge "Fertig" (bg-capacity-green/10 text-capacity-green)
       - 'problem' → red badge "Problem" (bg-capacity-red/10 text-capacity-red)

    2. Add sortable column header "IST-Status" after the existing "Status" column:
       - Use same pattern as other sortable headers
       - onClick={() => handleSort('istStatus')}
       - Include SortIcon component

    3. Add table cell in row mapping:
       - After computedStatus cell
       - Display: {getIstStatusBadge(order.istStatus)}

    4. Update colSpan in empty state row from 6 to 7
  </action>
  <verify>
    - Run dev server: `npm run dev`
    - Visit http://localhost:3000/orders
    - Verify IST-Status column appears between Status and Aktion columns
    - Verify orders with istStatus show colored badges
  </verify>
  <done>
    - IST-Status column visible in OrderTable
    - Column is sortable
    - Badges display correct colors: green (fertig), yellow (in_produktion), red (problem), grey (null)
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] IST-Status column visible in /orders table
- [ ] Color coding works correctly for all status values
- [ ] Sorting by IST-Status works
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- IST-Status column displays in OrderTable with proper color-coding
- Column is sortable like other columns
  </success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-status-column/07-01-SUMMARY.md`
</output>
