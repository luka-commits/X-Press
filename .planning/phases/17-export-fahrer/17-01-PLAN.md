---
phase: 17-export-fahrer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/route-utils.ts
  - src/components/versand/VersandOrderList.tsx
autonomous: true
---

<objective>
Add Google Maps navigation link export for optimized routes.

Purpose: Enable admins to copy a navigation link and share via WhatsApp - driver taps link, Google Maps opens with full route.
Output: "Link kopieren" button in route stats bar that generates and copies Google Maps URL.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-export-fahrer/17-RESEARCH.md
@.planning/phases/16-routenoptimierung/16-01-SUMMARY.md

@src/components/versand/VersandOrderList.tsx
@src/components/map/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route URL generation utilities</name>
  <files>src/lib/route-utils.ts</files>
  <action>
Create new file `src/lib/route-utils.ts` with:

1. `generateGoogleMapsUrl(orders: Array<{lieferLat: number; lieferLng: number}>): string`
   - Uses HTTPS Maps URLs format: `https://www.google.com/maps/dir/?api=1&...`
   - X-Press location (52.4046, 13.3718) as destination (return trip)
   - All delivery stops as waypoints (pipe-separated coordinates)
   - Includes `travelmode=driving&dir_action=navigate` for instant navigation
   - Round coordinates to 6 decimal places
   - Omit origin to use device's current location (driver already at X-Press)

2. `validateRouteUrl(url: string): { valid: boolean; message?: string }`
   - Check URL length <= 2048 characters
   - Return German error message if too long

3. `copyToClipboard(text: string): Promise<boolean>`
   - Use navigator.clipboard.writeText() when available
   - Fallback to textarea + execCommand('copy') for older browsers
   - Return true on success, false on failure

Export all three functions. Add JSDoc comments explaining Google Maps URL format.
  </action>
  <verify>File exists and exports three functions. TypeScript compiles: `npx tsc --noEmit`</verify>
  <done>Route URL generation utility module exists with all three functions exported</done>
</task>

<task type="auto">
  <name>Task 2: Add "Link kopieren" button to route stats bar</name>
  <files>src/components/versand/VersandOrderList.tsx</files>
  <action>
Modify `VersandOrderList.tsx`:

1. Import utilities: `import { generateGoogleMapsUrl, copyToClipboard, validateRouteUrl } from "@/lib/route-utils"`
2. Import Link2 icon: add `Link2` to lucide-react import

3. Add "Link kopieren" button inside the route stats bar (where `routeStats && routePlanningMode`):
   - Position: After the stops count, right-aligned
   - Icon: Link2
   - Label: "Link kopieren"
   - Style: Same as Optimieren button but green (bg-green-500 hover:bg-green-600)

4. Add `handleCopyRouteLink` async function:
   - Call `generateGoogleMapsUrl(routeOrdersForMap)`
   - Call `validateRouteUrl(url)` - if invalid, show error feedback and return
   - Call `copyToClipboard(url)`
   - On success: `setFeedback({ type: "success", message: "Navigationslink kopiert" })`
   - On failure: `setFeedback({ type: "error", message: "Link konnte nicht kopiert werden" })`

5. Wire button onClick to `handleCopyRouteLink`

The button should only appear when `routeStats` exists (i.e., after optimization).
  </action>
  <verify>`npm run build` succeeds. Route stats bar shows "Link kopieren" button after optimization.</verify>
  <done>"Link kopieren" button visible in route stats bar, clicking copies Google Maps URL to clipboard with feedback</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors (`npx tsc --noEmit`)
- [ ] Route stats bar shows "Link kopieren" button after route optimization
- [ ] Clicking button copies URL to clipboard (check with Cmd+V)
- [ ] Success feedback "Navigationslink kopiert" appears after copy
</verification>

<success_criteria>
- All tasks completed
- Route URL generated in correct Google Maps format
- Clipboard copy works with feedback
- URL validation prevents too-long URLs
</success_criteria>

<output>
After completion, create `.planning/phases/17-export-fahrer/17-01-SUMMARY.md`
</output>
