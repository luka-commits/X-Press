---
phase: 10-versand-api-liste
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/orders/[id]/versand/route.ts
  - src/app/api/versand/orders/route.ts
autonomous: true
---

<objective>
Create Versand API endpoints for shipping team workflow.

Purpose: Enable shipping team to mark orders as versandbereit/versendet and view orders filtered by delivery date with PLZ sorting.
Output: PATCH endpoint for versand status, GET endpoint for shipping order list.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-versand-datenmodell/09-01-SUMMARY.md

# Pattern reference - existing status PATCH endpoint:
@src/app/api/orders/[id]/status/route.ts

# Pattern reference - existing orders list endpoint:
@src/app/api/orders/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PATCH /api/orders/[id]/versand endpoint</name>
  <files>src/app/api/orders/[id]/versand/route.ts</files>
  <action>
Create PATCH endpoint mirroring the IstStatus pattern at /api/orders/[id]/status/route.ts:

1. Import NextRequest, NextResponse from 'next/server'
2. Import prisma from '@/lib/prisma'
3. Import VersandStatus from '@prisma/client'

4. PATCH handler:
   - Extract id from params (async pattern: `await params`)
   - Parse body for { versandStatus, versandKommentar }
   - Validate versandStatus is provided (400 if missing)
   - Validate versandStatus is valid enum value using Object.values(VersandStatus).includes()
   - Check order exists via prisma.auftrag.findUnique({ where: { auftragsnummer: id } })
   - Return 404 if not found
   - Update via prisma.auftrag.update with:
     - versandStatus: versandStatus as VersandStatus
     - versandKommentar: versandKommentar ?? null
     - versandUpdatedAt: new Date()
   - Return updated order

5. Error handling: try/catch with 500 response
6. German error messages following codebase convention
  </action>
  <verify>
curl -X PATCH http://localhost:3000/api/orders/GP25-7647/versand \
  -H "Content-Type: application/json" \
  -d '{"versandStatus": "versandbereit"}'
# Should return 200 with updated order including versandStatus

curl -X PATCH http://localhost:3000/api/orders/GP25-7647/versand \
  -H "Content-Type: application/json" \
  -d '{"versandStatus": "invalid"}'
# Should return 400 with error message
  </verify>
  <done>PATCH /api/orders/[id]/versand returns 200 for valid VersandStatus, 400 for invalid, 404 for non-existent order</done>
</task>

<task type="auto">
  <name>Task 2: Create GET /api/versand/orders endpoint</name>
  <files>src/app/api/versand/orders/route.ts</files>
  <action>
Create GET endpoint for shipping team order list following /api/orders pattern:

1. Import NextRequest, NextResponse from 'next/server'
2. Import prisma from '@/lib/prisma'
3. Import date-fns helpers: startOfDay, endOfDay, startOfWeek, endOfWeek
4. Import toZonedTime from 'date-fns-tz'

5. Define TIMEZONE = 'Europe/Berlin'

6. GET handler with query params:
   - deadline: 'today' | 'week' | 'all' (default: 'all')
   - versandStatus: 'offen' | 'versandbereit' | 'versendet' | 'all' (default: 'all')
   - sortBy: 'plz' | 'liefertermin' (default: 'plz')
   - sortOrder: 'asc' | 'desc' (default: 'asc')
   - page: number (default: 1)
   - limit: number (default: 25)

7. Build where clause:
   - status: 'aktiv' (only active orders for shipping)
   - Apply deadline filter (today/week using date-fns)
   - Apply versandStatus filter if not 'all'

8. Query with prisma.auftrag.findMany:
   - select fields: auftragsnummer, produkttyp, liefertermin, versandStatus, versandKommentar, versandUpdatedAt, lieferStrasse, lieferPlz, lieferOrt, lieferLand
   - include kunde: { select: { firma, name } }
   - orderBy: lieferPlz for PLZ sorting (route optimization) or liefertermin
   - pagination: skip/take

9. Also query prisma.auftrag.count for total

10. Return JSON: { orders, total, page, totalPages }

11. Error handling with German error message
  </action>
  <verify>
curl "http://localhost:3000/api/versand/orders"
# Should return paginated list with orders including address fields

curl "http://localhost:3000/api/versand/orders?deadline=today&sortBy=plz"
# Should return today's orders sorted by PLZ

curl "http://localhost:3000/api/versand/orders?versandStatus=versandbereit"
# Should return only orders with versandbereit status
  </verify>
  <done>GET /api/versand/orders returns paginated orders with Liefertermin filter, VersandStatus filter, and PLZ sorting</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] PATCH /api/orders/[id]/versand works with valid/invalid status
- [ ] GET /api/versand/orders returns orders with address fields
- [ ] Filters (deadline, versandStatus) work correctly
- [ ] PLZ sorting works for route optimization
</verification>

<success_criteria>

- PATCH endpoint validates VersandStatus enum
- PATCH endpoint updates versandStatus, versandKommentar, versandUpdatedAt
- GET endpoint returns shipping-relevant order data
- GET endpoint supports deadline filter (today, week)
- GET endpoint supports versandStatus filter
- GET endpoint sorts by PLZ by default
- Both endpoints follow established codebase patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/10-versand-api-liste/10-01-SUMMARY.md`
</output>
