---
phase: 03-mobile-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/MobileHeader.tsx
  - src/components/layout/MobileLayout.tsx
  - src/components/layout/index.ts
  - src/app/status/page.tsx
autonomous: true
---

<objective>
Create mobile-optimized layout components and /status route for production workers.

Purpose: Workers update order status from smartphones on the shop floor. Desktop navigation is irrelevant - they need a simple, touch-friendly interface.
Output: MobileLayout, MobileHeader components and /status page ready for Phase 4+ features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing patterns to follow:
@src/components/layout/MainLayout.tsx
@src/components/layout/Header.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MobileHeader component</name>
  <files>src/components/layout/MobileHeader.tsx</files>
  <action>
Create minimal header for mobile status page:
- Height: h-14 (smaller than desktop h-16)
- Show "XOS Status" title centered
- Include back button (left side) if optional `showBack` prop is true
- Blue accent color matching existing ghl-blue theme
- No navigation tabs (mobile workers don't need calendar/orders)
- Use 'use client' directive (needs useRouter for back button)
- Follow existing Header.tsx patterns for styling consistency
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>MobileHeader component exists with title and optional back button</done>
</task>

<task type="auto">
  <name>Task 2: Create MobileLayout component</name>
  <files>src/components/layout/MobileLayout.tsx, src/components/layout/index.ts</files>
  <action>
Create mobile-optimized layout wrapper:
- Use MobileHeader (not desktop Header)
- Safe area padding for mobile notches (pt-safe-top or env(safe-area-inset-top))
- Touch-friendly main content padding: p-4 (not p-6 like desktop)
- Full height: min-h-screen with flex-col
- Background: bg-ghl-bg (same as MainLayout)
- Export from index.ts barrel file alongside MainLayout

Interface:
```typescript
interface MobileLayoutProps {
  children: React.ReactNode;
  title?: string;  // Override "XOS Status" default
  showBack?: boolean;  // Show back button in header
}
```
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>MobileLayout component exported from @/components/layout</done>
</task>

<task type="auto">
  <name>Task 3: Create /status route with placeholder</name>
  <files>src/app/status/page.tsx</files>
  <action>
Create status page using MobileLayout:
- Use 'use client' directive (future phases will have interactive content)
- Wrap content in MobileLayout (no title prop, showBack=false)
- Placeholder content: simple card with "Auftrag suchen" text and note that search will be added in Phase 4
- Use existing Tailwind utilities and ghl-* colors
- Add comment marking where search component will go (Phase 4)

Structure:
```tsx
'use client';

import { MobileLayout } from '@/components/layout';

export default function StatusPage() {
  return (
    <MobileLayout>
      {/* Phase 4: Order search will be added here */}
      <div className="...">
        Placeholder content
      </div>
    </MobileLayout>
  );
}
```
  </action>
  <verify>Dev server shows /status page: npm run dev, then curl localhost:3000/status returns 200</verify>
  <done>/status route renders with MobileLayout and placeholder content</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes (TypeScript check)
- [ ] `npm run lint` passes (ESLint)
- [ ] `npm run build` succeeds
- [ ] /status route accessible and renders MobileLayout
</verification>

<success_criteria>

- MobileHeader and MobileLayout components created
- Components follow existing patterns (ghl-* colors, Tailwind)
- /status route exists and uses mobile layout
- All verification checks pass
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-layout/03-01-SUMMARY.md`
</output>
