---
phase: 20-versand-reports
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [src/components/reports/VersandView.tsx, src/components/reports/PlzChart.tsx, src/components/reports/index.ts, src/app/reports/page.tsx]
autonomous: true
---

<objective>
Create VersandView component with shipping metrics visualization and enable Versand-Reports tab.

Purpose: Display shipping analytics in a visual format - KPI cards for delivery performance, bar chart for PLZ distribution.
Output: Working Versand-Reports tab on /reports page with live data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-versand-reports/20-01-SUMMARY.md

# Existing patterns to follow:
@src/components/reports/AnalyticsView.tsx
@src/components/reports/VolumeChart.tsx
@src/app/reports/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlzChart and VersandView components</name>
  <files>src/components/reports/PlzChart.tsx, src/components/reports/VersandView.tsx, src/components/reports/index.ts</files>
  <action>
**PlzChart.tsx:**
Create horizontal bar chart component for PLZ distribution using Recharts BarChart.
- Props: data (array of { plz: string, count: number }), loading (boolean)
- Follow VolumeChart pattern: loading spinner, empty state, ResponsiveContainer with fixed height
- Use horizontal layout (layout="vertical") for better PLZ label readability
- German locale not needed (PLZ are just numbers)
- Blue bars (#3b82f6) consistent with VolumeChart

**VersandView.tsx:**
Create shipping analytics view following AnalyticsView pattern:
- Uses DateRangePicker (import from same module)
- Fetches from /api/reports/versand?from=X&to=Y
- Displays 3 KPI cards in a grid:
  1. Liefertreue: onTimePercent% (with onTime/total as subtext)
  2. Verspatungen: late count (with avgDelayDays if > 0)
  3. Versendungen: total count
- Below cards: PlzChart showing top 10 PLZ regions
- Handle loading and error states

**index.ts:**
Add exports for PlzChart and VersandView
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>PlzChart and VersandView components created and exported</done>
</task>

<task type="auto">
  <name>Task 2: Enable Versand-Reports tab on reports page</name>
  <files>src/app/reports/page.tsx</files>
  <action>
Update reports page:
1. Import VersandView from @/components/reports
2. Change versand tab from disabled: true to disabled: false
3. Replace placeholder content with VersandView component when activeTab === 'versand'

Remove the "(Demnachst)" text from the tab.
  </action>
  <verify>Visit /reports, click Versand-Reports tab, see VersandView with data</verify>
  <done>Versand-Reports tab is clickable and shows shipping analytics</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Versand-Reports tab is enabled and clickable
- [ ] VersandView displays KPI cards and PLZ chart
- [ ] DateRangePicker works and data updates on change
</verification>

<success_criteria>

- All tasks completed
- VersandView shows delivery metrics and PLZ distribution
- Tab navigation works between all three report views
- No errors or warnings in console
</success_criteria>

<output>
After completion, create `.planning/phases/20-versand-reports/20-02-SUMMARY.md`
</output>
