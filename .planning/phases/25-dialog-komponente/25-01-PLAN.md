---
phase: 25-dialog-komponente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/dialog.tsx]
autonomous: true
---

<objective>
Install shadcn/ui Dialog component for use in KPI overlay feature.

Purpose: Provide reusable Dialog component that will display order lists when KPI cards are clicked.
Output: Dialog component installed at src/components/ui/dialog.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# shadcn/ui is already configured in project
@components.json

# Existing UI components for reference
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui Dialog component</name>
  <files>src/components/ui/dialog.tsx</files>
  <action>
    Run shadcn/ui CLI to install Dialog component:
    ```bash
    npx shadcn@latest add dialog --yes
    ```

    This will create src/components/ui/dialog.tsx with:
    - Dialog, DialogTrigger, DialogContent, DialogHeader, DialogFooter
    - DialogTitle, DialogDescription, DialogClose
    - Radix UI primitives underneath

    The --yes flag auto-confirms overwrite prompts.
  </action>
  <verify>
    - File exists: src/components/ui/dialog.tsx
    - TypeScript compiles: npm run build
    - Component exports Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription
  </verify>
  <done>Dialog component installed and TypeScript compilation passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] src/components/ui/dialog.tsx exists
- [ ] `npm run build` succeeds without errors
- [ ] Dialog component can be imported: `import { Dialog } from "@/components/ui/dialog"`
</verification>

<success_criteria>

- Dialog component installed via shadcn/ui CLI
- No TypeScript or build errors
- Component ready for use in Phase 27
  </success_criteria>

<output>
After completion, create `.planning/phases/25-dialog-komponente/25-01-SUMMARY.md`
</output>
